---
phase: 04-story-and-ship
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - findings/FINDINGS.md
  - README.md
  - data/README.md
  - .planning/PROJECT.md
  - .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md
  - docs/ftn-schema-audit.md
  - analysis/01_exploratory.py
  - analysis/01_exploratory.ipynb
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08

must_haves:
  truths:
    - "findings/FINDINGS.md exists as a memo with TL;DR (~250 words; 4 highlight bullets; names the 4 pre-registered situations explicitly; D-15 divergence previewed in one sentence; top-3+bottom-3+league-avg leaderboard rollup), 6 named insights in locked sequence (#1 leaderboard headline -> #2 D-15 H-vs-KL divergence -> #3 S1 PA x blitz pre-registered chi-square -> #4 S3 PA x blitz exploratory chi-square -> #5 red-zone pressure differential -> #6 team-level pressure beat), 3 thematic methodology blocks (metric choice & calibration / sensitivity & robustness / sample-size discipline & pre-registered firewall), 4 appendix tables (T1 per-team-per-situation predictability cells, T2 KL leaderboard with rank-delta vs H, T3 STAT-08 sensitivity rank-delta, T4 S1+S3 contingency tables paired), 5-item Limitations section (L1 no paid FTN coverage labels / L2 4-season scope / L3 PA rate on S1 is 1.235% strategic abandonment / L4 FTN charter subjectivity with IRR-not-published acknowledgment / L6 tabular-only no tracking), and FTN + nflverse CC-BY-SA attribution."
    - "Every claim in FINDINGS.md states N inline (DOC-02); each insight follows the locked claim -> evidence -> implication/caveat 3-sentence template (D-05) with a mandatory third-sentence caveat on every insight."
    - "README.md is hand-rewritten with all placeholder slots filled in (the regex `<[A-Z_]{4,}>` returns zero matches), hero PNG `findings/images/01_predictability_ranking.png` embedded above the fold, 3-4 stat-first bullet findings, Mermaid data-flow diagram (DOC-04), 5-command setup block + reproducibility budget callout (DOC-05), 6-term glossary (down, distance, EPA, blitz [defined as `n_blitzers > 0`], RPO, predictability index) (DOC-06), FTN + nflverse CC-BY-SA attribution + Known Issues section noting nfl_data_py archival (DOC-07)."
    - "data/README.md (DOC-08) documents the gitignored `.db`, the regen path `python -m etl.run`, and the parquet cache layout in `data/raw/`."
    - "Cross-doc reconciliation sweep complete (D-48): 5 sites (PROJECT.md L58, .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md D-02, docs/ftn-schema-audit.md operational definition, README glossary, analysis/01_exploratory.py L163 SQL string) all carry `n_blitzers > 0` (or `n_blitzers >= 1` in code), and a SCOPED grep for `n_blitzers > 4` across user-facing surfaces + code (PROJECT.md as a special case INSIDE .planning/, plus README.md, findings/FINDINGS.md, data/README.md, docs/*.md, analysis/*.py, queries/*.sql, etl/*.py, schema/*.sql, scripts/*.py — but EXCLUDING .planning/phases/* and .planning/STATE.md which intentionally retain the stale phrase as audit trail) returns zero matches per D-49."
    - "README hook reconciled to cross-situation framing per D-48 (the existing 'predictable than others on third-and-long' phrasing is generalized since the hero chart aggregates across all 4 pre-registered situations, NOT S1 only)."
  artifacts:
    - path: "findings/FINDINGS.md"
      provides: "DOC-01 + DOC-02 memo (TL;DR + 6 insights + 3-block methodology + 4 appendix tables + 5 limitations + attribution)"
      contains: "## TL;DR"
      min_lines: 200
    - path: "README.md"
      provides: "DOC-03 hand-rewrite + DOC-04 Mermaid + DOC-05 setup + DOC-06 glossary + DOC-07 attribution + Known Issues"
      contains: "```mermaid"
      min_lines: 80
    - path: "data/README.md"
      provides: "DOC-08 — gitignored DB regen path + parquet cache layout"
      contains: "python -m etl.run"
    - path: "docs/ftn-schema-audit.md"
      provides: "Anchor narrative — operational definition reconciled to `n_blitzers > 0`"
      contains: "n_blitzers > 0"
    - path: ".planning/PROJECT.md"
      provides: "Project reference — Domain primer L58 reconciled to `n_blitzers > 0`"
      contains: "n_blitzers > 0"
    - path: "analysis/01_exploratory.py"
      provides: "Phase 3 exploratory notebook — stale SQL string at L163 reconciled to `n_blitzers >= 1`"
      contains: "n_blitzers >= 1"
  key_links:
    - from: "README.md"
      to: "findings/images/01_predictability_ranking.png"
      via: "Markdown image reference (relative path findings/images/01_predictability_ranking.png) above the fold"
      pattern: "findings/images/01_predictability_ranking\\.png"
    - from: "README.md"
      to: "findings/FINDINGS.md"
      via: "Methodology section link to the full memo"
      pattern: "findings/FINDINGS\\.md"
    - from: "findings/FINDINGS.md"
      to: "findings/images/01_predictability_ranking.png"
      via: "Markdown image reference (relative path images/01_predictability_ranking.png) at the headline insight"
      pattern: "images/01_predictability_ranking\\.png"
    - from: "findings/FINDINGS.md"
      to: "data/nfl_defensive_tendencies.db"
      via: "Methodology block 1 + appendix tables T1-T4 cite specific N counts and SQL filters from competitive_plays"
      pattern: "competitive_plays"
---

<objective>
Author the recruiter-facing memo + README + cross-doc prose reconciliation. FINDINGS.md tells the analytical story; README.md is the 30-second recruiter glance with the hero PNG above the fold; data/README.md documents the gitignored DB. The 5-site D-48 reconciliation sweep + D-49 SCOPED grep verification eliminate the stale `n_blitzers > 4` prose lag from Phase 3 from user-facing surfaces + code (the `.planning/phases/*` and `.planning/STATE.md` audit-trail references that intentionally retain the stale phrase are explicitly out of D-49 scope).

Purpose: This is the "story" half of Phase 4. The analytical numbers exist (Phase 3 + Plan 04-01 S3 cells); this plan turns them into a memo a non-football-fluent recruiter can skim in 30 seconds and read in 5 minutes. The cross-doc sweep is bundled here because one author keeping prose-coherence across all four files (plus the fifth code site) with the FINDINGS-narrative context fresh is the discuss-locked design (D-48).

Output: `findings/FINDINGS.md` (greenfield memo); `README.md` (hand-rewrite over existing skeleton); `data/README.md` (extended); 5-site cross-doc reconciliation (PROJECT.md L58, 03-CONTEXT.md D-02, docs/ftn-schema-audit.md operational definition, README glossary entry, analysis/01_exploratory.py L163 SQL string); D-49 post-sweep SCOPED grep verification with zero `n_blitzers > 4` matches across user-facing surfaces + code surfaced in 04-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-story-and-ship/04-CONTEXT.md
@.planning/phases/04-story-and-ship/04-PRE-DISCUSS-BRIEF.md
@.planning/phases/04-story-and-ship/04-PATTERNS.md
@.planning/phases/04-story-and-ship/04-01-SUMMARY.md
@.planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md
@.planning/phases/03-analytical-layer-sql-python/03-02-SUMMARY.md
@.planning/phases/03-analytical-layer-sql-python/03-03-SUMMARY.md
@docs/analysis-plan.md
@docs/ftn-schema-audit.md
@README.md
@data/README.md

<phase_3_numbers_to_narrate>
<!-- Verified against the live DB in 04-PRE-DISCUSS-BRIEF.md and 03-03-SUMMARY.md.
     Plan 04-02 narrates these numbers; the executor verifies each value at
     execution time before committing prose. -->

Insight #1 (leaderboard headline):
  Top-3:    PHI 23.53, SF 23.48, IND 22.37
  Bottom-3 (by score): MIA 5.91, TB 4.07, MIN 1.53
  League average: ~12.5 (compute empirically from QUERY-07 leaderboard mean -- exact value lands in 04-01-SUMMARY.md)

Insight #2 (D-15 divergence):
  Spearman rho = -0.111  (p = 0.546)
  Pre-registered gate: rho >= 0.85 -> FAILS
  Top-5 disagreers (rank-delta >= 22):
    MIN  rank_H=32  rank_KL=1   delta=31
    TB   rank_H=31  rank_KL=2   delta=29
    PIT  rank_H=26  rank_KL=3   delta=23
    MIA  rank_H=30  rank_KL=8   delta=22
    DET  rank_H=27  rank_KL=5   delta=22

Insight #3 (S1 PA x blitz, pre-registered):
  chi^2 = 3.4643   p = 0.0627
  OR = 0.648   95% CI = [0.418, 1.003]
  Wilson CI on P(blitz | PA=1, S1) = 0.248  [0.176, 0.336]   N=109
  Paired P(blitz | PA=0, S1) = 0.337
  Observed gap = -8.94pp   (clears pre-registered 5pp gate directionally)
  N total S1 pass = 8,825   PA=1 stratum = 109 (1.235%)
  Expected min cell = 36.6  (chi-square assumption holds; no Fisher fallback)

Insight #4 (S3 PA x blitz, EXPLORATORY -- numbers materialize from Plan 04-01 Task 4):
  Universe verified: N=18,609; PA=1 N=8,652 (46.49%); PA=0 N=9,957
  Numerical results (chi^2, p, OR, Wilson CI, OR delta vs S1, direction-agreement):
    available in `analysis/02_predictability_modeling.py` cell stdout after Plan 04-01 ran;
    surfaced in 04-01-SUMMARY.md.
  D-13 5-scenario decision rule classifies the result; D-14 3-sentence shape:
    Sentence 1 -- S3 result with numbers.
    Sentence 2 -- explicit cross-situation comparison with S1 using the OR delta.
    Sentence 3 -- implication (defensive behavior or limits of the pre-registered analysis).
  Author the prose at execution time per D-15 (CONTEXT) -- pre-locked rule + structure
  protects against post-hoc rationalization without forcing stiff phrasing.

Insight #5 (red-zone pressure differential):
  RZ pressure rate = 36.2%
  Midfield pressure rate = 26.7%
  Gap = +9.5pp
  Source: queries/03_red_zone_vs_midfield.sql

Insight #6 (team-level pressure beat):
  DET 52.3% S1 blitz rate as the candidate (verify DET is still the league leader at
  execution time per Claude's Discretion -- if a different team has emerged, update the
  prose accordingly; the exact 52.3% comes from queries/05_third_long_pressure_tendencies.sql).

D-16 sensitivity (methodology block 2):
  Spearman rho = 0.982 between with-filter and without-filter leaderboards
  Max |rank delta| = 4
  Top-5 identical with one PHI<->HOU swap.

D-14 calibration (methodology block 1):
  n_blitzers semantics: counts EXTRA rushers above 4-man front (NOT total rushers)
  Corrected operational definition: `n_blitzers > 0` (or `>= 1` in code)
  Stale: `n_blitzers > 4`  (matched only 7 of 58,178 plays = 0.012%)
  Corrected league baseline blitz rate: 29.45% (consistent with public benchmarks)

L3 prose constraint (D-19): MUST explain WHY PA collapses on 3rd-and-long --
  PA requires credible run threat; 3rd-and-7+ removes threat; PA strategically abandoned.
  The 1.235% rate is real strategic constraint, not data quality.

L4 prose constraint (D-20): MUST acknowledge limitation's own boundary --
  FTN does not publish IRR for is_play_action / is_rpo so we cannot bound the
  impact of charter subjectivity quantitatively. Frame as honest about what isn't
  known rather than papering over.

L5 lives in README Known Issues (NOT FINDINGS limitations) per D-18.
</phase_3_numbers_to_narrate>

<five_site_reconciliation_sweep>
<!-- D-48 + D-49: ALL 5 sites must be updated; THEN run the SCOPED grep.
     CRITICAL — D-49 SCOPE rationale: a naive repo-wide grep for `n_blitzers > 4`
     produces ~20 legitimate matches across `.planning/phases/01-*`,
     `.planning/phases/03-*` SUMMARYs, `.planning/phases/04-*` plan files
     (these very plans!), `.planning/STATE.md` (the Decisions log), and the
     planning docs that explain the bug + correction. Those references ARE the
     audit trail and MUST stay. D-49 is therefore scoped to user-facing surfaces
     + code, with PROJECT.md as a special-case INCLUSION because it's the only
     user-facing file that lives inside `.planning/`.

     IN-SCOPE for D-49 negative grep:
       - README.md, findings/FINDINGS.md, data/README.md
       - docs/*.md  (e.g., docs/ftn-schema-audit.md, docs/analysis-plan.md)
       - analysis/*.py
       - queries/*.sql
       - etl/*.py
       - schema/*.sql
       - scripts/*.py
       - .planning/PROJECT.md  (SPECIAL CASE — user-facing reference doc that
         happens to live inside .planning/)

     OUT-OF-SCOPE for D-49 negative grep (intentional audit trail):
       - .planning/phases/01-*  (Phase 1 SUMMARYs that document the original wrong threshold)
       - .planning/phases/03-*  (Phase 3 SUMMARYs/CONTEXT that document the catch + correction)
       - .planning/phases/04-*  (these plan files; reference the stale phrase to explain the fix)
       - .planning/STATE.md     (Decisions D-14 narrates the bug + correction)
       - .git/, __pycache__/, venv/, .venv/, node_modules/, .venv-fresh/
-->

Site 1 -- .planning/PROJECT.md L58 (Domain primer operational definition):
  Current text contains: `(n_blitzers > 4)`
  Replace with:          `(n_blitzers > 0)`

Site 2 -- .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md D-02:
  Current text contains: `n_blitzers > 4`
  Replace with:          `n_blitzers > 0`
  Add a brief deviation footnote in 03-CONTEXT.md noting that the original
  Phase 3 lock was corrected during Phase 3 Wave 2 execution (commit 4cb60de
  + bf2aba0); preserve the original decision text but mark it superseded so
  the audit trail stays intact. Format: leave D-02 as-is and append a one-line
  "**Phase 3 calibration (post-execution):** corrected to `n_blitzers > 0` --
  see STATE.md D-14 for rationale and 04-02-PLAN.md D-48 for the cross-doc
  sweep that landed this." sentence DIRECTLY UNDER the D-02 paragraph.
  (NOTE: 03-CONTEXT.md is INSIDE .planning/phases/03-* which is OUT of scope
  for the D-49 negative grep -- but the sweep still updates the inline reference
  to keep the working decision text current. The audit-trail sentence ALONE
  isn't sufficient; the active decision text must reflect the corrected operational
  definition. Other stale references inside .planning/phases/03-* SUMMARYs are
  preserved as historical record.)

Site 3 -- docs/ftn-schema-audit.md (operational definition for the anchor):
  Pattern-mapper found this near line 78. The audit doc currently treats
  `n_blitzers` as if `> 4` were the blitz boolean (mirroring the nflfastR
  n_pass_rushers convention). Replace `n_blitzers > 4` with `n_blitzers > 0`
  and add a 1-2 sentence "Calibration note (Phase 3 post-execution)" paragraph
  explaining: FTN's n_blitzers counts EXTRA rushers above the base 4-man front
  (max observed = 6); the original `> 4` reuse of the nflfastR convention was
  semantically wrong; the corrected boolean produces a 29.45% league rate
  consistent with public sports-analytics benchmarks. Cite STATE.md D-14 by name.

Site 4 -- README.md glossary entry for "Blitz":
  This is created NEW in this plan's Task 2 (no existing glossary in README).
  The new glossary entry MUST use:
    "Blitz. When a defense sends extra rushers beyond its base 4-man front.
     Operationally in this project: `n_blitzers > 0` (any FTN-charted extra rusher)."

Site 5 -- analysis/01_exploratory.py line ~163 (the 5th site pattern-mapper found):
  Current SQL string contains: `WHEN n_blitzers > 4 THEN 1.0 ELSE 0.0`
  Replace with:                `WHEN n_blitzers >= 1 THEN 1.0 ELSE 0.0`
  (Use `>= 1` here to match analysis/02_predictability_modeling.py:270 which uses
  `n_blitzers >= 1` -- semantically identical to `> 0` but matches the existing
  S1 cell convention in the file the executor most recently touched.)
  Re-execute the notebook to confirm the SQL still produces sensible numbers
  (the league_anchor blitz_rate per situation should match the corrected
  ~29% league baseline; previously the broken threshold returned ~0.01%).
  Then clear outputs via `nbconvert --clear-output --inplace` before commit.

D-49 post-sweep SCOPED grep:
  After all 5 sites are reconciled, run from repo root using an EXPLICIT
  ALLOW-LIST of files (cleaner than directory-glob exclusions; immune to future
  reorganization of .planning/phases/*):

    # Negative check — scoped to user-facing surfaces + code; expects ZERO matches.
    # .planning/phases/* and .planning/STATE.md are OUT OF SCOPE (audit trail).
    # PROJECT.md is INCLUDED as a special-case (only user-facing file inside .planning/).
    # Comment the rationale inline in the verify block so future maintainers
    # don't accidentally widen scope.
    : > /tmp/d49_unscoped.txt
    for f in README.md findings/FINDINGS.md data/README.md \
             docs/ftn-schema-audit.md docs/analysis-plan.md \
             .planning/PROJECT.md \
             .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md \
             analysis/01_exploratory.py \
             analysis/02_predictability_modeling.py \
             analysis/03_visualizations.py \
             analysis/_common.py analysis/_style.py; do
      [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
    done
    # Also sweep code globs that may not be enumerable file-by-file:
    for g in 'queries/*.sql' 'etl/*.py' 'schema/*.sql' 'scripts/*.py'; do
      for f in $g; do
        [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
      done
    done
    test ! -s /tmp/d49_unscoped.txt

  Positive-presence check (catches typos in the corrected text; expects >=1
  match per file):
    grep -c "n_blitzers > 0" .planning/PROJECT.md   # >=1
    grep -c "n_blitzers > 0" .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md  # >=1
    grep -c "n_blitzers > 0" docs/ftn-schema-audit.md   # >=1
    grep -cE "(n_blitzers > 0|n_blitzers >= 1)" README.md    # >=1 (glossary)
    grep -cE "(n_blitzers > 0|n_blitzers >= 1)" analysis/01_exploratory.py  # >=1

  Surface BOTH grep outputs in 04-02-SUMMARY.md.
</five_site_reconciliation_sweep>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author findings/FINDINGS.md (greenfield memo: TL;DR + 6 insights + 3-block methodology + 4 appendix tables + 5 limitations + attribution)</name>
  <files>findings/FINDINGS.md</files>
  <read_first>
    - .planning/phases/04-story-and-ship/04-CONTEXT.md sections A (D-01..D-05), B (D-06..D-09), D (D-16..D-20) — THE memo blueprint
    - .planning/phases/04-story-and-ship/04-PRE-DISCUSS-BRIEF.md (the factual record on D-14..D-17 + n_blitzers correction; this is THE input to insights #2, #3, #4 prose and the L3 + methodology block 1)
    - .planning/phases/04-story-and-ship/04-01-SUMMARY.md (the S3 chi-square numbers from Plan 04-01 Task 4; insight #4 narrates these)
    - .planning/phases/03-analytical-layer-sql-python/03-03-SUMMARY.md (predictability leaderboard top-5/bottom-5 + KL gate failure rho = -0.111 + S1 chi-square + STAT-08 sensitivity rho = 0.982)
    - .planning/phases/03-analytical-layer-sql-python/03-02-SUMMARY.md (DET 52.3% S1 candidate; RZ 36.2% vs midfield 26.7%)
    - docs/ftn-schema-audit.md (the closest in-repo voice analog; opening paragraph pattern at lines 1-14)
    - docs/analysis-plan.md (the closest in-repo structure analog; sample-size table format at lines 154-161 transfers to FINDINGS appendix T1)
    - .planning/phases/04-story-and-ship/04-PATTERNS.md "findings/FINDINGS.md" section (voice analog opening, structure analog falsifiable-claim format, sample-size table format, attribution block, anti-patterns to avoid)
    - CLAUDE.md "Audience Voice" section (memo style, numbers first, N inline, mandatory Limitations section, NO emoji headers, NO exclamation points, NO "Welcome to my project!" tone, NO AI-template phrases like "comprehensive", "leverages", "cutting-edge", "in this article we will explore", "I'm excited to share", "This section discusses…")
  </read_first>
  <action>
    Author `findings/FINDINGS.md` in a single editorial pass. The structure is fully locked; the prose voice is fully locked; only specific sentence-level wording is authored at execution time per D-15 (CONTEXT). Working memo length target: ~3000 words; trim aggressively.

    Concrete file structure (DO NOT deviate from this section order or the H2 header text):

    ```markdown
    # NFL Defensive Tendencies — Findings

    ## TL;DR

    [Framing paragraph (~150 words) that names the 4 pre-registered situations
     explicitly per D-07 (S1 3rd-and-long, S2 red zone, S3 1st-and-10, S4
     2nd-and-medium); previews the D-15 divergence in ONE sentence per D-08;
     names the dataset scope (4 seasons 2022-2025; 32 NFL defenses; 105,556
     competitive plays in the analytical universe; 58,178 competitive pass
     plays joined to FTN charting). Voice: numbers-first, no exclamation,
     no "Welcome", no emoji.]

    [4 highlight bullets per D-06 + D-09. The first bullet is the leaderboard
     rollup in the EXACT format from D-09:]
    - Top 3 (most predictable): PHI (23.5), SF (23.5), IND (22.4). Bottom 3: MIA (5.9), TB (4.1), MIN (1.5). League average: 12.5. (N=128 cells; min cell N=92.)
    - [Bullet 2: D-15 divergence one-line preview pointing forward to insight #2]
    - [Bullet 3: S1 PA x blitz directional finding -- p=0.063, OR=0.648 [0.418, 1.003], -8.94pp gap; pre-registered]
    - [Bullet 4: red-zone pressure differential or DET S1 beat -- pick the more recruiter-recognizable one]

    Total TL;DR target: ~250 words per D-06.

    ## 1. Predictability Leaderboard

    [Insight #1: claim -> evidence -> implication/caveat per D-05.
     Sentence 1 (claim): The 32 NFL defenses range from PHI at 23.5 to MIN at 1.5
       on a 0-100 predictability index aggregated across the 4 pre-registered
       situations.
     Sentence 2 (evidence): expanded per D-05 -- top-3 + bottom-3 + league-mean;
       reference the hero chart by file path.
     Sentence 3 (implication/caveat): mandatory per D-05 -- frame as scope
       (e.g., "Per-team rates vary across situations; appendix T1 carries the
       32x4 cell table") or generality.
     Embed the hero PNG with markdown image syntax:
       ![Predictability leaderboard, 32 NFL defenses, 2022-2025](images/01_predictability_ranking.png)
     This file path is RELATIVE to findings/FINDINGS.md -- the PNG is at
     findings/images/01_predictability_ranking.png so the relative path is
     `images/01_predictability_ranking.png` (no leading findings/).]

    ## 2. Two Definitions of "Predictable" Disagree

    [Insight #2: claim -> evidence -> implication/caveat per D-05.
     Claim: Spearman rho = -0.111 between H/log(2)-concentration and
       KL-from-league-baseline rankings; the pre-registered rho >= 0.85
       validation gate fails.
     Evidence (expanded per D-05): a 2-row rank-delta example pair like
       "MIN ranks 32nd on H concentration but 1st on KL deviation; PIT ranks
       26th on H but 3rd on KL." Reference the scatter image:
       ![KL-vs-H rank-rank scatter](images/02_kl_vs_h_scatter.png)
     Implication: this is structural, not a bug -- centers of "extreme"
       differ between H (50/50) and KL (29.45% league baseline). Refer to
       methodology block 1 for the explanation.
     Sentence 3 caveat: mandatory; frame as scope ("This pattern holds for the
       four-season scope; whether the centers reconverge in a longer panel is
       not addressed").]

    ## 3. Pre-registered: Defenses Blitz Less Against Play-Action on 3rd-and-Long (S1)

    [Insight #3: pre-registered tag in the section title PER D-04. Numbers:
     chi^2 = 3.4643, p = 0.0627, OR = 0.648 [0.418, 1.003], Wilson CI on
     P(blitz | PA=1, S1) = 0.248 [0.176, 0.336] (N=109), paired
     P(blitz | PA=0, S1) = 0.337, observed gap = -8.94pp.
     Sentence 3 caveat MANDATORY: small N(PA=1)=109; effect size CI grazes the
     null; p marginally outside alpha = 0.05; framed as suggestive directional
     evidence, not confirmed effect. Note this is S1 only; S3 follow-up below.]

    ## 4. Exploratory: Same Test on 1st-and-10 (S3) — Not Pre-registered

    [Insight #4: exploratory tag in the section title per D-04. The exact
     numbers come from analysis/02_predictability_modeling.py S3 cells (Plan
     04-01 Task 4 stdout); they are surfaced in 04-01-SUMMARY.md.
     Author the prose at execution time per D-15 (CONTEXT). Use D-13's 5-scenario
     decision rule to pick the framing. Use D-14's 3-sentence structural shape:
       Sentence 1 -- S3 result with numbers.
       Sentence 2 -- explicit cross-situation comparison with S1 via the OR delta.
       Sentence 3 (implication/caveat MANDATORY) -- what this tells us about
         defensive behavior or the limits of the pre-registered analysis.
     Section title MUST contain the literal string "Exploratory" and a label
     equivalent to "not pre-registered in docs/analysis-plan.md".]

    ## 5. Red-Zone Pressure Differential

    [Insight #5: RZ 36.2% vs midfield 26.7%, gap = +9.5pp. Source: QUERY-03.
     N counts inline. Caveat sentence mandatory.]

    ## 6. Team-Level Pressure Beat

    [Insight #6: DET 52.3% S1 blitz rate (verify at execution time per
     CONTEXT Claude's Discretion -- if a different team has emerged as league
     leader, update the prose; the exact 52.3% comes from QUERY-05).
     Caveat sentence mandatory.]

    ## Methodology

    ### Block 1: Metric Choice and Calibration (D-14 + H/log(2) derivation)

    [3 paragraphs ~per D-16 block 1.
     Paragraph 1: D-14 calibration story -- FTN n_blitzers semantics
       (extra rushers above 4-man front, NOT total); the > 4 vs > 0 catch;
       why the > 4 misread crossed columns from nflfastR's n_pass_rushers
       convention; corrected blitz rate 29.45% consistent with public benchmarks.
     Paragraph 2: H/log(2) formula -- normalized Shannon entropy over the
       blitz/no-blitz binary support; bounded 0 to log(2); divided by log(2)
       to give a unit-interval scalar; 1 - H/log(2) inverts so high =
       predictable; * 100 maps to 0-100. Why this beats raw entropy
       (raw is bounded by log(k) which differs across teams with different
       observed-category counts).
     Paragraph 3: 0-100 mapping rationale -- portfolio-friendly scale; aligns
       hero chart's fixed 0-100 axis with metric semantics; no truncation.]

    ### Block 2: Sensitivity and Robustness (D-16 sensitivity + KL secondary)

    [2 paragraphs.
     Paragraph 1: STAT-08 sensitivity check -- Spearman rho = 0.982 between
       with-filter (competitive_plays) and without-filter (plays JOIN ftn_play)
       leaderboards; max |rank delta| = 4; top-5 identical except one PHI<->HOU
       swap. The headline is not an artifact of the wp filter.
     Paragraph 2: contrast with insight #2 -- one definition (H/log(2)) is
       robust to the filter; the other definition (KL from league baseline)
       isn't even consistent with itself across rankings. The H/log(2) headline
       is the more stable choice.]

    ### Block 3: Sample-Size Discipline and the Pre-registered Firewall

    [2 paragraphs.
     Paragraph 1: tiered N>=30 / N>=100 / N>=15-with-explicit-narrative-flag
       framework; min_n_filter behavior (drops cells; does not raise); all
       128 (team x situation) cells clear N>=30 by margin (smallest cell N=92).
     Paragraph 2: pre-registered firewall -- the 4 situations from
       docs/analysis-plan.md are the falsifiable claims; S3 PA x blitz is
       explicitly outside the firewall as exploratory follow-up; the
       methodological-status labels (pre-registered vs exploratory) are how
       multiple-comparisons discipline shows up in the memo.]

    ## Appendix

    ### T1: Per-team-per-situation predictability cells

    [Multi-column table per D-17. NOT a heatmap. Columns:
     | Team | S1 score (N) | S2 score (N) | S3 score (N) | S4 score (N) | Aggregate |
     32 rows. Source: analysis/02_predictability_modeling.py leaderboard CSV
     output OR direct re-query. Either author the table verbatim from the
     leaderboard data or include a one-line note "Generated from
     analysis/02_predictability_modeling.py output; reproducible via
     `python -m etl.run` + Restart-and-Run-All on the notebook" if the
     table is too long for inline.]

    ### T2: KL leaderboard with rank-delta vs H/log(k)

    [32-row table per D-17. Columns:
     | Team | KL rank | H/log(2) rank | Delta |
     Sorted by KL rank. Evidences D-15 directly.]

    ### T3: STAT-08 sensitivity rank-delta

    [32-row table per D-17. Columns:
     | Team | With-filter rank | Without-filter rank | Delta |
     Spearman rho = 0.982 stated in caption. Evidences D-16.]

    ### T4: S1 + S3 contingency tables paired

    [Two 2x2 tables per D-17.
     S1 table:
     |        | blitz | no_blitz | row total |
     | PA=1   |   27  |    82    |    109    |
     | PA=0   | 2,938 |  5,778   |   8,716   |
     | total  | 2,965 |  5,860   |   8,825   |

     S3 table: numbers from 04-01-SUMMARY.md (a3, b3, c3, d3 from the S3 cells).
     Caption notes the pre-registered/exploratory distinction.]

    ## Limitations

    [5 named items per D-18 in the EXACT order L1, L2, L3, L4, L6 (skip L5
     which lives in README Known Issues per D-18 + DOC-07).

     - **L1: No paid FTN coverage labels.** The full Cover-0 through Cover-6
       and man/zone taxonomy is part of FTN's paid commercial product, not
       the CC-BY-SA public subset distributed via nflverse. This project
       anchors on `n_blitzers`, `n_pass_rushers`, `is_play_action`,
       `n_offense_backfield`, etc. — the public columns FTN does include.

     - **L2: 4-season scope.** 2022-2025 (through Super Bowl LX). Pre-2022
       FTN charting is not in the public dataset, so longitudinal trend
       analysis beyond 4 seasons is not addressed.

     - **L3: PA rate on 3rd-and-long is 1.235%.** This is why the S1 chi-square
       is underpowered (PA=1 stratum N=109). The 1.235% rate reflects a real
       strategic constraint, not a data-quality issue: play-action requires a
       credible run threat to freeze linebackers; on 3rd-and-7+, the run is no
       longer a credible play call, so the PA fake doesn't fool a defense
       already locked into pass coverage. PA is therefore strategically
       abandoned. The S3 (1st-and-10) exploratory chi-square in insight #4
       exists as the inferentially-powered follow-up where PA actually fires
       (PA rate on S3 = 46.49%).

     - **L4: FTN charter subjectivity.** FTN's `is_play_action`, `is_rpo`, and
       similar boolean fields are charter judgment calls. FTN does not publish
       inter-rater reliability statistics for these fields, so the impact of
       charter subjectivity on the chi-square findings cannot be bounded
       quantitatively. This is a limitation of what is and isn't known about
       the data, not a finding-specific weakness.

     - **L6: Tabular-only data.** No player-tracking or film coverage. The
       observable defensive surface is what FTN charts; defensive looks that
       exist on film but aren't tagged in FTN's public subset are invisible
       here.]

    ## Attribution

    Data sources:
    - **FTN charting** via the [nflverse](https://github.com/nflverse) project, distributed under [CC-BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/).
    - **nflfastR** play-by-play via `nfl_data_py==0.3.3`.

    This work is shared under the same CC-BY-SA 4.0 license inheritance for any data-derived claim or chart; the project's own code is MIT (see `LICENSE`).
    ```

    Voice rules (re-read CLAUDE.md "Audience Voice" before authoring):
    - NO emoji section headers (NEVER `## :rocket: TL;DR` or any emoji at section level).
    - NO exclamation points anywhere.
    - NO "Welcome to my project!" / "I built this!" / first-person-effusive tone.
    - NO AI-template phrases: "comprehensive", "robust" (when used as marketing fluff), "leverages", "cutting-edge", "in this article we will explore", "I am excited to share", "Welcome to". The verify block enforces a regex sweep for these.
    - Numbers FIRST: "The Bills blitz 38% on 3rd-and-long (N=247)." NOT "The Bills are an aggressive blitzing defense."
    - State sample size INLINE with every claim.
    - Treat "data" as singular ("the public FTN dataset", "the data is consistent with") -- match docs/ftn-schema-audit.md and docs/analysis-plan.md convention.
    - All `n_blitzers` references use `> 0` (or `>= 1` if quoting code); NEVER `> 4`.
    - Use regular hyphens not en-dashes ("2022-2025" not "2022–2025") -- D-35 convention applied repo-wide for GitHub Unicode rendering consistency.
    - Use `rho` spelled out (or the Greek `ρ` if the executor prefers; both render correctly on GitHub) for Spearman rho. Be consistent within the file.

    DO NOT include a glossary in FINDINGS.md (glossary lives in README per DOC-06). DO NOT include the L5 nfl_data_py archival note in FINDINGS limitations (lives in README Known Issues per D-18). DO NOT use `<details>` HTML expandable. DO NOT add badge soup. DO NOT use footnotes (`[^1]` -- GitHub renders inconsistently per 04-PATTERNS).
  </action>
  <verify>
    <automated>
    set -e
    test -f findings/FINDINGS.md
    test $(wc -l < findings/FINDINGS.md) -gt 100
    grep -qE '^## TL;DR' findings/FINDINGS.md
    grep -qE '^## 1\.' findings/FINDINGS.md
    grep -qE '^## 2\.' findings/FINDINGS.md
    grep -qE '^## 3\.' findings/FINDINGS.md
    grep -qE '^## 4\.' findings/FINDINGS.md
    grep -qE '^## 5\.' findings/FINDINGS.md
    grep -qE '^## 6\.' findings/FINDINGS.md
    grep -qE '^## Methodology' findings/FINDINGS.md
    grep -qE '^### Block 1' findings/FINDINGS.md
    grep -qE '^### Block 2' findings/FINDINGS.md
    grep -qE '^### Block 3' findings/FINDINGS.md
    grep -qE '^## Appendix' findings/FINDINGS.md
    grep -qE '^### T1' findings/FINDINGS.md
    grep -qE '^### T2' findings/FINDINGS.md
    grep -qE '^### T3' findings/FINDINGS.md
    grep -qE '^### T4' findings/FINDINGS.md
    grep -qE '^## Limitations' findings/FINDINGS.md
    grep -qE '^## Attribution' findings/FINDINGS.md
    grep -qF 'L1' findings/FINDINGS.md
    grep -qF 'L2' findings/FINDINGS.md
    grep -qF 'L3' findings/FINDINGS.md
    grep -qF 'L4' findings/FINDINGS.md
    grep -qF 'L6' findings/FINDINGS.md
    ! grep -qF 'L5' findings/FINDINGS.md
    grep -qF '1.235%' findings/FINDINGS.md
    grep -qF 'inter-rater reliability' findings/FINDINGS.md
    grep -qF 'images/01_predictability_ranking.png' findings/FINDINGS.md
    grep -qF 'images/02_kl_vs_h_scatter.png' findings/FINDINGS.md
    grep -qF 'CC-BY-SA' findings/FINDINGS.md
    grep -qF 'nflverse' findings/FINDINGS.md
    grep -qF 'nfl_data_py' findings/FINDINGS.md
    ! grep -qF 'n_blitzers > 4' findings/FINDINGS.md
    ! grep -qE '^#+\s.*[!]' findings/FINDINGS.md
    ! grep -qE '^#+\s.*[\xF0\x9F\x80-\xF0\x9F\x99]' findings/FINDINGS.md
    ! grep -qF 'Welcome' findings/FINDINGS.md
    ! grep -qE '<[A-Z_]{4,}>' findings/FINDINGS.md
    grep -qE 'Exploratory' findings/FINDINGS.md
    grep -qE '(pre-registered|Pre-registered)' findings/FINDINGS.md
    # Voice/tone enforcement -- CLAUDE.md "Audience Voice" non-negotiables.
    # Catches AI-template fluff phrases that recruiters read as low-effort boilerplate.
    # `grep -E` (extended regex, POSIX) is supported by the bash that GitHub Actions
    # runs `run: |` blocks under; word boundaries via [[:<:]]/[[:>:]] are NOT portable
    # across GNU/BSD grep, so we use lookaround-free patterns with explicit space/punctuation.
    ! grep -qiE '(^|[^a-z])comprehensive([^a-z]|$)' findings/FINDINGS.md
    ! grep -qiE '(^|[^a-z])leverages?([^a-z]|$)' findings/FINDINGS.md
    ! grep -qiE 'cutting[- ]edge' findings/FINDINGS.md
    ! grep -qiE 'in this (article|section|memo) we will' findings/FINDINGS.md
    ! grep -qiE "i (am|'m) (excited|thrilled)" findings/FINDINGS.md
    ! grep -qiE 'welcome to' findings/FINDINGS.md
    </automated>
  </verify>
  <done>
    `findings/FINDINGS.md` exists with the EXACT structure: H1 title; H2 TL;DR; 6 H2 numbered insights "## 1." through "## 6."; H2 Methodology with 3 H3 blocks "### Block 1/2/3"; H2 Appendix with 4 H3 tables T1/T2/T3/T4; H2 Limitations with L1/L2/L3/L4/L6 (NO L5); H2 Attribution. Hero PNG embedded via `images/01_predictability_ranking.png`. Scatter PNG embedded via `images/02_kl_vs_h_scatter.png`. CC-BY-SA + nflverse + nfl_data_py attributions present. No emoji headers, no exclamation points in headers, no "Welcome", no `n_blitzers > 4` anywhere, no placeholder slots, "1.235%" inline (proves L3 strategic explanation lands), "inter-rater reliability" inline (proves L4 boundary acknowledgment lands), "Exploratory" tag on insight #4 section title, "pre-registered" or "Pre-registered" tag on insight #3 section title. AI-template phrases ("comprehensive", "leverages", "cutting-edge", "in this article we will…", "I am excited", "Welcome to") all absent (case-insensitive). > 100 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-rewrite README.md (hero PNG above the fold + Mermaid diagram + 5-command setup + 6-term glossary + attribution + Known Issues; reconcile hook to cross-situation framing per D-48)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current skeleton with placeholder slots `<MOST_PREDICTABLE_DEFENSE_2025>`, `<SCORE>`, `<DELTA>` and the existing hook prose; preserve the section order; fill all placeholders)
    - findings/FINDINGS.md (just authored in Task 1; README cites the same numbers and links to FINDINGS for the full memo)
    - .planning/phases/04-story-and-ship/04-CONTEXT.md sections F (D-32 hero title -- README hook MUST reconcile to match), J (D-48 cross-doc reconciliation -- README hook + glossary entry are sites 4 of 5)
    - .planning/phases/04-story-and-ship/04-PATTERNS.md "README.md" section (verbatim Mermaid template, 5-command setup pattern, 6-term glossary pattern with `n_blitzers > 0` for Blitz, Attribution + Known Issues blocks)
    - .planning/PROJECT.md "Reproducibility budget" section (the >= 5 commands, <= 10 minutes constraint that DOC-05 encodes)
    - CLAUDE.md "Audience Voice" + "Locked Stack" + Windows venv activation notes
    - findings/images/01_predictability_ranking.png (must already exist from Plan 04-01 -- the README embeds it above the fold)
  </read_first>
  <action>
    Hand-rewrite `README.md` filling all placeholders and adding the missing sections. Preserve the existing section order: Hook -> Findings preview -> Architecture -> Setup -> Glossary -> Methodology -> Limitations -> Attribution -> Known Issues. Each section is hand-written; no AI-template tone.

    Use the following content patterns. Numbers come from FINDINGS.md TL;DR (which Task 1 just wrote); ensure README and FINDINGS agree numerically.

    ```markdown
    # NFL Defensive Tendencies

    ## Hook

    [Reconciled per D-48 to cross-situation framing -- the existing hook reads
     "predictable than others on third-and-long" which is INCONSISTENT with
     the hero chart that aggregates across all 4 pre-registered situations.
     Recommended phrasing matching D-32 chart title:]

    Some NFL defenses are more predictable than others. This project ranks all 32 across four pre-registered situations (3rd-and-long, red zone, 1st-and-10, 2nd-and-medium) using four seasons (2022-2025, through Super Bowl LX) of nflfastR play-by-play and FTN charting. The public FTN columns cover pressure, play-fakery, and personnel; they don't include FTN's paid Cover-shell or man/zone labels, so this project anchors on what's there.

    Result: a 0-100 predictability index where higher means more predictable. PHI leads the league at 23.5, about 11 points above the league average of 12.5.

    ![Predictability leaderboard, 32 NFL defenses, 2022-2025](findings/images/01_predictability_ranking.png)

    [Replace `<MOST_PREDICTABLE_DEFENSE_2025>` -> "PHI",
             `<SCORE>` -> "23.5",
             `<DELTA>` -> "about 11 points" or "11.0 points" depending on exact league avg.
     Verify these against findings/FINDINGS.md TL;DR before committing.]

    ## Findings preview

    [3-4 stat-first bullets per DOC-03. Each bullet has N inline.]
    - PHI 23.5 / SF 23.5 / IND 22.4 lead the predictability leaderboard; MIA 5.9 / TB 4.1 / MIN 1.5 trail. (32 defenses, 4 seasons, 128 cells.)
    - On 3rd-and-long, defenses blitz 24.8% against play-action versus 33.7% otherwise -- an 8.94pp gap that grazes statistical significance (chi^2=3.46, p=0.063, OR=0.65 [0.42, 1.00]; N=109 for the PA stratum).
    - Red-zone pressure rate (36.2%) runs 9.5 percentage points hotter than midfield (26.7%). (Source: queries/03_red_zone_vs_midfield.sql.)
    - DET led the league in 3rd-and-long blitz rate at 52.3%. (Verify and update at execution time per CONTEXT Claude's Discretion.)

    ## Architecture

    ```mermaid
    flowchart LR
        A[nfl_data_py 0.3.3] -->|FTN + pbp pull| B[data/raw/*.parquet]
        B -->|whitelist + join| C[(SQLite: nfl_defensive_tendencies.db)]
        C -->|filter wp 0.05-0.95| D[competitive_plays view]
        D -->|8 SQL queries| E[queries/*.sql]
        E -->|pd.read_sql_query| F[Jupyter notebooks]
        F -->|matplotlib + _style.py| G[findings/images/*.png]
        G -->|hero PNG| H[README + FINDINGS.md]
    ```

    [Note: GitHub's Mermaid renderer occasionally diverges from local previews
     on edge cases. The diagram is eyeball-verified during private staging
     per Plan 04-03 (D-41).]

    ## Setup

    ```bash
    git clone https://github.com/<user>/nfl-defensive-tendencies.git
    cd nfl-defensive-tendencies
    python3.11 -m venv .venv && source .venv/bin/activate    # Windows: py -3.11 -m venv .venv && .venv\Scripts\activate
    pip install -r requirements.txt
    python -m etl.run                                         # ~2-5 min cold cache; regenerates data/nfl_defensive_tendencies.db
    ```

    Reproducibility budget: 5 commands, under 10 minutes on a stock laptop after the first `nfl_data_py` pull. The notebooks reproduce all numbers and figures via Restart and Run All on `analysis/01_exploratory.ipynb`, `analysis/02_predictability_modeling.ipynb`, and `analysis/03_visualizations.ipynb` after the ETL runs.

    [Note: requirements.txt pinning + nfl_data_py archival means the install is
     reproducible only as long as PyPI continues to host the pinned versions;
     see Known Issues below.]

    ## Glossary

    - **Down.** A play attempt. The offense gets four downs to gain 10 yards or score; failure turns the ball over.
    - **Distance.** Yards needed for a first down on the next play. "3rd-and-long" means 3rd down with 7 or more yards to gain.
    - **EPA (Expected Points Added).** A play-level value capturing how much a play improved (or hurt) the offense's expected scoring outcome.
    - **Blitz.** When a defense sends extra rushers beyond its base 4-man front. Operationally in this project: `n_blitzers > 0` (any FTN-charted extra rusher).
    - **RPO (Run-Pass Option).** A play where the QB reads a defender post-snap and chooses run or pass.
    - **Predictability index.** A 0-100 score per defense per situation, derived from normalized Shannon entropy `H/log(2)` over the blitz/no-blitz binary; 0 means uniform 50/50 (truly random), 100 means deterministic.

    ## Methodology

    Brief: predictability index = `(1 - H/log(2)) * 100` aggregated across 4 pre-registered situations with sample-size-weighted means; cells require N >= 30. The blitz boolean is `n_blitzers > 0` -- FTN's `n_blitzers` counts extra rushers above the 4-man base, not total rushers; the corrected league baseline blitz rate is 29.45%. Full methodology, including the Spearman validation gate failure between H/log(2) and KL leaderboards, the STAT-08 sensitivity check (rho=0.982), and the pre-registered/exploratory firewall protocol, lives in [findings/FINDINGS.md](findings/FINDINGS.md).

    ## Limitations

    Short: this project anchors on the public FTN charting subset, which does not include Cover-0..6 or man/zone labels (those ship with FTN's paid product). Four-season scope (2022-2025). Charter subjectivity on `is_play_action` / `is_rpo` is acknowledged but not bounded quantitatively because FTN doesn't publish IRR for those fields. Tabular-only -- no tracking or film coverage. Full limitations list (5 items, including the structural reason play-action collapses to a 1.235% rate on 3rd-and-long) lives in [findings/FINDINGS.md](findings/FINDINGS.md#limitations).

    ## Attribution

    Data sources:
    - **FTN charting** via the [nflverse](https://github.com/nflverse) project, distributed under [CC-BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/).
    - **nflfastR** play-by-play via `nfl_data_py==0.3.3`.

    This work is shared under the same CC-BY-SA 4.0 license inheritance for any data-derived claim or chart; the project's own code is MIT (see [LICENSE](LICENSE)).

    ## Known Issues

    - **`nfl_data_py` archived 2025-09-25.** Upstream stopped accepting changes; this project pins `==0.3.3` and accepts the maintenance risk for v1. The `numpy<2.0` constraint is a downstream consequence (the package references `np.float_`, removed in NumPy 2). The maintained successor is `nflreadpy`; migration is a v2 candidate (see `.planning/PROJECT.md`).
    ```

    DO NOT add any badge above the title. DO NOT add CONTRIBUTING.md / CODE_OF_CONDUCT.md mentions (D-38). DO NOT add `<details>` HTML expandables (visual noise per 04-PATTERNS). DO NOT use exclamation points anywhere. DO NOT use emoji section headers.

    Verify the placeholder regex (SHIP-08) holds: `! grep -qE '<[A-Z_]{4,}>' README.md` exits 0. The 5-character `<user>` placeholder in the git clone URL has 4 lowercase chars and does NOT match the regex (regex requires uppercase A-Z). Confirm with the grep command directly.

    Verify hero PNG path: the file `findings/images/01_predictability_ranking.png` must exist (Plan 04-01 produced it); the README references it via `findings/images/01_predictability_ranking.png` (relative path from repo root, since README.md sits at repo root).

    Verify hook reconciliation: README hook MUST NOT contain "predictable than others on third-and-long" verbatim (the stale framing); it MUST contain a cross-situation phrasing that names the 4 pre-registered situations. Acceptance check below greps for both.
  </action>
  <verify>
    <automated>
    set -e
    test -f README.md
    test $(wc -l < README.md) -gt 50
    grep -qE '^# NFL Defensive Tendencies' README.md
    grep -qE '^## Hook' README.md
    grep -qE '^## Findings preview' README.md
    grep -qE '^## Architecture' README.md
    grep -qE '^## Setup' README.md
    grep -qE '^## Glossary' README.md
    grep -qE '^## Methodology' README.md
    grep -qE '^## Limitations' README.md
    grep -qE '^## Attribution' README.md
    grep -qE '^## Known Issues' README.md
    grep -qF '```mermaid' README.md
    grep -qF 'flowchart LR' README.md
    grep -qF 'competitive_plays' README.md
    grep -qF 'findings/images/01_predictability_ranking.png' README.md
    grep -qF 'pip install -r requirements.txt' README.md
    grep -qF 'python -m etl.run' README.md
    grep -qE '\*\*Down\.\*\*' README.md
    grep -qE '\*\*Distance\.\*\*' README.md
    grep -qE '\*\*EPA' README.md
    grep -qE '\*\*Blitz\.\*\*' README.md
    grep -qE '\*\*RPO' README.md
    grep -qE '\*\*Predictability index\.\*\*' README.md
    grep -qF 'n_blitzers > 0' README.md
    grep -qF 'CC-BY-SA' README.md
    grep -qF 'nflverse' README.md
    grep -qF 'nfl_data_py' README.md
    grep -qF 'archived 2025-09-25' README.md
    ! grep -qF 'Welcome' README.md
    ! grep -qF 'n_blitzers > 4' README.md
    ! grep -qE '<[A-Z_]{4,}>' README.md
    grep -qE 'four pre-registered situations|3rd-and-long.*red zone.*1st-and-10' README.md
    ! grep -qE '^# .*[!]' README.md
    ! grep -qE '^## .*[!]' README.md
    test -f findings/images/01_predictability_ranking.png
    </automated>
  </verify>
  <done>
    `README.md` is a hand-rewritten file with all 9 H2 sections in correct order (Hook, Findings preview, Architecture, Setup, Glossary, Methodology, Limitations, Attribution, Known Issues); hero PNG embedded above the fold via `findings/images/01_predictability_ranking.png`; Mermaid diagram with `flowchart LR` orientation present; 5-command setup block with `pip install -r requirements.txt` and `python -m etl.run`; 6-term glossary with all 6 bold-headed entries; "Blitz" glossary entry uses `n_blitzers > 0` operational definition; CC-BY-SA + nflverse + nfl_data_py archival noted in Known Issues; placeholder regex `<[A-Z_]{4,}>` returns zero matches; `n_blitzers > 4` returns zero matches; cross-situation hook framing present (names the 4 pre-registered situations); no emoji headers, no exclamation points in headers, no "Welcome" tone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend data/README.md (DOC-08) + execute the 5-site cross-doc reconciliation sweep + run D-49 SCOPED post-sweep grep verification</name>
  <files>data/README.md, .planning/PROJECT.md, .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md, docs/ftn-schema-audit.md, analysis/01_exploratory.py, analysis/01_exploratory.ipynb</files>
  <read_first>
    - data/README.md (existing 7 lines; extend, do not replace per 04-PATTERNS)
    - .planning/PROJECT.md line 58 (the Domain primer with `(n_blitzers > 4)`)
    - .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md D-02 (the locked decision text with `n_blitzers > 4`)
    - docs/ftn-schema-audit.md (operational definition section -- pattern-mapper found this near line 78; verify exact line at execution time)
    - analysis/01_exploratory.py lines 155-175 (the SQL string with `WHEN n_blitzers > 4 THEN 1.0 ELSE 0.0` -- the 5th reconciliation site flagged by the pattern mapper)
    - .planning/phases/04-story-and-ship/04-CONTEXT.md D-48, D-49 (the sweep instructions and grep verification)
    - .planning/phases/04-story-and-ship/04-PATTERNS.md "data/README.md" + "n_blitzers > 0" sections (the existing data/README content + the corrected operational definition pattern)
    - .planning/phases/04-story-and-ship/04-PRE-DISCUSS-BRIEF.md section 1.4 (the prose-lag list that this task closes out)
    - The `<five_site_reconciliation_sweep>` block in this plan's <context> -- specifically the D-49 SCOPE rationale (audit-trail references inside .planning/phases/* and .planning/STATE.md are intentionally OUT of scope; PROJECT.md is INCLUDED as a special case)
  </read_first>
  <action>
    Three sub-tasks in this single plan task. Execute them in order; the post-sweep grep depends on all 5 site edits being complete.

    Sub-task A — data/README.md (DOC-08): Extend (do NOT replace) the existing file to the structure documented in 04-PATTERNS. Final file should look like:

    ```markdown
    # Data

    This directory holds the project's data layer. The SQLite database is the analytical source of truth and is regenerated from a fresh clone via the ETL pipeline.

    ## Files

    - `data/nfl_defensive_tendencies.db` — **gitignored** (200-400 MB exceeds GitHub's 100 MiB hard limit). Regenerate via `python -m etl.run`. Contains 1,139 games / 197,362 plays / 185,215 ftn_play rows / 105,556 competitive_plays / 58,178 competitive pass plays joined to FTN, 2022-2025.
    - `data/raw/*.parquet` — **gitignored**. The ETL caches `nfl_data_py` pulls here on first run; subsequent runs are idempotent. Layout: `pbp_<year>.parquet` + `ftn_<year>.parquet` per year (8 files, ~80 MB combined).
    - `data/README.md` — this file.

    ## Regeneration path

    ```bash
    python -m etl.run
    ```

    Cold cache: ~2-5 minutes. Warm cache (parquet files already populated): under 60 seconds.

    ## Schema

    DDL lives in `schema/`:
    - `schema/01_create_tables.sql` — `games`, `plays`, `ftn_play` tables with `(game_id, play_id)` PKs.
    - `schema/02_indexes.sql` — composite indexes on `(down, ydstogo, yardline_100)` and `(defteam, season)`.
    - `schema/03_views.sql` — `competitive_plays` view (the project-wide analytical universe; `play_type IN ('pass','run')` AND `wp BETWEEN 0.05 AND 0.95` AND not 2-minute drill / OT).

    Every analytical query in `queries/*.sql` reads from `competitive_plays`, never from `plays` directly.
    ```

    Sub-task B — Five-site cross-doc reconciliation sweep (D-48):

    Site 1: `.planning/PROJECT.md` line 58 — replace `(n_blitzers > 4)` with `(n_blitzers > 0)` (single substitution, in-place).

    Site 2: `.planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md` D-02 — leave the original D-02 paragraph text intact (audit trail) and APPEND a single sentence directly under it: `**Phase 3 calibration (post-execution):** corrected to ` + "`n_blitzers > 0`" + ` — see STATE.md D-14 for rationale and Plan 04-02 (D-48) for the cross-doc reconciliation sweep that landed this.` Then ALSO update the inline `n_blitzers > 4` reference inside D-02 itself to `n_blitzers > 0` so the D-02 decision text doesn't carry a stale operational definition. The audit-trail sentence captures the change history; the decision text itself reflects the current operational definition.

    Site 3: `docs/ftn-schema-audit.md` — replace `n_blitzers > 4` with `n_blitzers > 0` at the operational-definition site (pattern-mapper noted line ~78; grep to confirm exact line). ALSO append a "Calibration note (Phase 3 post-execution)" subsection after the operational-definition paragraph with prose like:

    ```markdown
    ### Calibration note (Phase 3 post-execution)

    The original operational definition specified `n_blitzers > 4`, reusing the standard nflfastR convention where `n_pass_rushers > 4` (5 or more rushers) defines a blitz. That convention applies to nflfastR's `n_pass_rushers` column, which is a total rusher count. FTN's `n_blitzers` is a different column with a different encoding: it counts extra rushers above the base 4-man defensive front (max observed value = 6 across 4 seasons). Any `n_blitzers >= 1` therefore means a blitz was sent. Under the corrected `n_blitzers > 0` boolean, the league-wide blitz rate on competitive pass plays is 29.45% (N=17,131 / 58,178), consistent with public sports-analytics benchmarks. See `.planning/STATE.md` D-14 for the full audit trail.
    ```

    Site 4: README.md glossary entry for "Blitz" — already corrected in Task 2 of this plan; verify the entry contains the literal string `n_blitzers > 0`.

    Site 5: `analysis/01_exploratory.py` line ~163 (verify exact line via grep) — inside the inline SQL string, replace `WHEN n_blitzers > 4 THEN 1.0 ELSE 0.0` with `WHEN n_blitzers >= 1 THEN 1.0 ELSE 0.0`. Use `>= 1` (not `> 0`) to match the existing convention in `analysis/02_predictability_modeling.py:270` and the S3 cells from Plan 04-01 Task 4. After the edit, run `jupytext --sync analysis/01_exploratory.py` to update the paired .ipynb. Re-execute the notebook end-to-end (`jupyter nbconvert --to notebook --execute --inplace analysis/01_exploratory.ipynb`) and visually inspect the printed `league_anchor` table — the `blitz_rate` column should now show values in the 0.20-0.50 range across the 4 situations (under the broken threshold this column was effectively zero across the board). Then clear outputs: `jupyter nbconvert --clear-output --inplace analysis/01_exploratory.ipynb`. Verify ruff clean: `ruff check analysis/01_exploratory.py` exits 0.

    Sub-task C — D-49 post-sweep SCOPED grep verification:

    Run from repo root using the EXPLICIT ALLOW-LIST defined in the plan's
    `<five_site_reconciliation_sweep>` block. The negative grep targets ONLY
    user-facing surfaces + code; `.planning/phases/*` and `.planning/STATE.md`
    are intentionally OUT of scope because they retain the stale phrase as
    audit trail (~20 legitimate matches there, all explaining the catch +
    correction). PROJECT.md is INCLUDED because it's the only user-facing file
    inside `.planning/`.

    ```bash
    # .planning/phases/* and .planning/STATE.md intentionally reference the stale
    # n_blitzers > 4 as audit trail; D-49 scope is user-facing surfaces + code only.
    : > /tmp/d49_unscoped.txt
    for f in README.md findings/FINDINGS.md data/README.md \
             docs/ftn-schema-audit.md docs/analysis-plan.md \
             .planning/PROJECT.md \
             .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md \
             analysis/01_exploratory.py \
             analysis/02_predictability_modeling.py \
             analysis/03_visualizations.py \
             analysis/_common.py analysis/_style.py; do
      [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
    done
    for g in 'queries/*.sql' 'etl/*.py' 'schema/*.sql' 'scripts/*.py'; do
      for f in $g; do
        [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
      done
    done
    cat /tmp/d49_unscoped.txt          # show contents (expected empty)
    test ! -s /tmp/d49_unscoped.txt    # assert empty

    # Positive-presence checks (catch typos in the corrected text).
    grep -c 'n_blitzers > 0' .planning/PROJECT.md
    grep -c 'n_blitzers > 0' .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md
    grep -c 'n_blitzers > 0' docs/ftn-schema-audit.md
    grep -cE '(n_blitzers > 0|n_blitzers >= 1)' README.md
    grep -cE '(n_blitzers > 0|n_blitzers >= 1)' analysis/01_exploratory.py
    ```

    Both grep outputs are surfaced in 04-02-SUMMARY.md per D-49. Zero matches expected on the negative check; at least one match per file expected on the positive check.

    DO NOT touch SQL files in `queries/*.sql` (they were corrected in Phase 3 Plan 03-02 commit 4cb60de and are already at `n_blitzers >= 1`). DO NOT touch `analysis/02_predictability_modeling.py` for `n_blitzers` content (already corrected in Phase 3 Plan 03-03 commit bf2aba0). DO NOT add new entries to the .planning/STATE.md Decisions table — D-14 already documents this; the cross-doc sweep is the EXECUTION of D-14, not a new decision. DO NOT widen the D-49 negative-grep scope to include `.planning/phases/*` or `.planning/STATE.md` — the audit-trail references there are by design.
  </action>
  <verify>
    <automated>
    set -e
    # Sub-task A: data/README.md
    test -f data/README.md
    grep -qF 'data/nfl_defensive_tendencies.db' data/README.md
    grep -qF 'gitignored' data/README.md
    grep -qF 'python -m etl.run' data/README.md
    grep -qF 'data/raw/' data/README.md
    grep -qF 'pbp_<year>.parquet' data/README.md
    grep -qF '105,556' data/README.md
    grep -qF 'competitive_plays' data/README.md

    # Sub-task B: 5-site reconciliation
    grep -qF 'n_blitzers > 0' .planning/PROJECT.md
    grep -qF 'n_blitzers > 0' .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md
    grep -qF 'Phase 3 calibration (post-execution)' .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md
    grep -qF 'n_blitzers > 0' docs/ftn-schema-audit.md
    grep -qF 'Calibration note' docs/ftn-schema-audit.md
    grep -qF 'n_blitzers > 0' README.md
    grep -qF 'n_blitzers >= 1' analysis/01_exploratory.py

    # Sub-task C: SCOPED post-sweep grep verification (D-49).
    # Rationale: .planning/phases/* and .planning/STATE.md intentionally reference
    # the stale `n_blitzers > 4` as audit trail (the SUMMARYs and CONTEXT files
    # explain the bug + correction). D-49 scope is user-facing surfaces + code only.
    # PROJECT.md is INCLUDED as a special case (only user-facing file inside .planning/).
    : > /tmp/d49_unscoped.txt
    for f in README.md findings/FINDINGS.md data/README.md \
             docs/ftn-schema-audit.md docs/analysis-plan.md \
             .planning/PROJECT.md \
             .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md \
             analysis/01_exploratory.py \
             analysis/02_predictability_modeling.py \
             analysis/03_visualizations.py \
             analysis/_common.py analysis/_style.py; do
      [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
    done
    for g in 'queries/*.sql' 'etl/*.py' 'schema/*.sql' 'scripts/*.py'; do
      for f in $g; do
        [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_unscoped.txt
      done
    done
    cat /tmp/d49_unscoped.txt
    test ! -s /tmp/d49_unscoped.txt

    # Positive checks -- each prose file has at least one corrected reference.
    test $(grep -c 'n_blitzers > 0' .planning/PROJECT.md) -ge 1
    test $(grep -c 'n_blitzers > 0' .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md) -ge 1
    test $(grep -c 'n_blitzers > 0' docs/ftn-schema-audit.md) -ge 1
    test $(grep -cE '(n_blitzers > 0|n_blitzers >= 1)' README.md) -ge 1
    test $(grep -c 'n_blitzers >= 1' analysis/01_exploratory.py) -ge 1

    # Code: 01_exploratory.ipynb has cleared outputs (file size sanity check).
    test $(stat -c %s analysis/01_exploratory.ipynb 2>/dev/null || stat -f %z analysis/01_exploratory.ipynb) -lt 50000
    ruff check analysis/01_exploratory.py
    </automated>
  </verify>
  <done>
    `data/README.md` extended with Files / Regeneration path / Schema sections, specific filename `data/nfl_defensive_tendencies.db`, row counts (1,139 / 197,362 / 185,215 / 105,556 / 58,178), parquet cache layout. PROJECT.md L58 + 03-CONTEXT.md D-02 + docs/ftn-schema-audit.md operational definition + README glossary all carry `n_blitzers > 0`; analysis/01_exploratory.py L163 carries `n_blitzers >= 1`. The "Phase 3 calibration (post-execution)" sentence in 03-CONTEXT.md preserves the audit trail. The "Calibration note (Phase 3 post-execution)" subsection in docs/ftn-schema-audit.md explains the FTN-vs-nflfastR column-encoding catch and cites STATE.md D-14. SCOPED D-49 negative grep over the user-facing-surfaces-+-code allow-list (excluding .planning/phases/* and .planning/STATE.md audit trail; including PROJECT.md as special case) returns zero matches. analysis/01_exploratory.ipynb has cleared outputs and ruff is clean.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run a single combined verification:

```bash
# Both prose surfaces are SHIP-08 clean.
! grep -qE '<[A-Z_]{4,}>' README.md
! grep -qE '<[A-Z_]{4,}>' findings/FINDINGS.md

# 6 named insights in FINDINGS.md.
test $(grep -cE '^## [1-6]\.' findings/FINDINGS.md) -eq 6

# Cross-doc sweep is complete and verifiable (D-49) -- SCOPED grep over the
# user-facing-surfaces-+-code allow-list (audit-trail references inside
# .planning/phases/* and .planning/STATE.md are intentionally OUT of scope;
# PROJECT.md is INCLUDED as the special-case user-facing file inside .planning/).
: > /tmp/d49_negative.txt
for f in README.md findings/FINDINGS.md data/README.md \
         docs/ftn-schema-audit.md docs/analysis-plan.md \
         .planning/PROJECT.md \
         .planning/phases/03-analytical-layer-sql-python/03-CONTEXT.md \
         analysis/01_exploratory.py \
         analysis/02_predictability_modeling.py \
         analysis/03_visualizations.py \
         analysis/_common.py analysis/_style.py; do
  [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_negative.txt
done
for g in 'queries/*.sql' 'etl/*.py' 'schema/*.sql' 'scripts/*.py'; do
  for f in $g; do
    [ -f "$f" ] && grep -Hn 'n_blitzers > 4' "$f" >> /tmp/d49_negative.txt
  done
done
cat /tmp/d49_negative.txt
test ! -s /tmp/d49_negative.txt

# Hero PNG and scatter PNG referenced from BOTH FINDINGS and README.
grep -qF 'images/01_predictability_ranking.png' findings/FINDINGS.md
grep -qF 'images/02_kl_vs_h_scatter.png' findings/FINDINGS.md
grep -qF 'findings/images/01_predictability_ranking.png' README.md

# 5 limitations (L1, L2, L3, L4, L6) and L5 explicitly absent from FINDINGS.
for L in L1 L2 L3 L4 L6; do grep -qF "$L" findings/FINDINGS.md || echo "MISSING $L"; done
! grep -qF 'L5' findings/FINDINGS.md

# README has no shields.io badge soup (>5 badges -- 0 is the de facto target;
# CI/Python/license badges are the ceiling per 04-PATTERNS).
test $(grep -cE 'shields\.io' README.md) -le 5
```

Phase 4 / 04-02 success: FINDINGS.md memo authored with 6 insights + 3-block methodology + 4 appendix tables + 5 limitations + attribution; README.md hand-rewritten with hero PNG above the fold + Mermaid + 5-command setup + 6-term glossary + attribution + Known Issues; data/README.md extended; 5-site cross-doc reconciliation sweep complete with zero `n_blitzers > 4` matches across user-facing surfaces + code (audit-trail references inside .planning/phases/* and .planning/STATE.md preserved by design).
</verification>

<success_criteria>
- DOC-01: `findings/FINDINGS.md` exists structured TL;DR -> 6 named insights -> methodology appendix -> limitations -> FTN+nflverse attribution.
- DOC-02: Every FINDINGS claim states N inline; tiered N≥30/100/15-flag discipline visible in methodology block 3.
- DOC-03: README.md hand-written, hero PNG above the fold, 3-4 stat-first bullet findings, no AI-template emoji headers, no "Welcome" tone, no AI-template fluff phrases ("comprehensive", "leverages", "cutting-edge", etc.) per CLAUDE.md Audience Voice.
- DOC-04: README has Mermaid `flowchart LR` data-flow diagram (NOT file tree).
- DOC-05: README has 5-command setup block + reproducibility budget callout (≤5 commands, ≤10 min).
- DOC-06: README has 6-term glossary defining down, distance, EPA, blitz, RPO, predictability index in plain English; "Blitz" entry uses the corrected `n_blitzers > 0` operational definition.
- DOC-07: README includes FTN + nflverse CC-BY-SA attribution + Known Issues section noting `nfl_data_py` archival.
- DOC-08: `data/README.md` documents gitignored DB, regen path, parquet cache layout.
- D-48 cross-doc reconciliation: PROJECT.md L58 + 03-CONTEXT.md D-02 + docs/ftn-schema-audit.md operational definition + README glossary entry + analysis/01_exploratory.py L163 SQL string all corrected.
- D-49 post-sweep SCOPED grep: zero matches for `n_blitzers > 4` across the user-facing-surfaces-+-code allow-list (README.md, findings/FINDINGS.md, data/README.md, docs/*.md, analysis/*.py, queries/*.sql, etl/*.py, schema/*.sql, scripts/*.py, plus the special-case .planning/PROJECT.md and .planning/phases/03-*/03-CONTEXT.md). `.planning/phases/01-*`, `.planning/phases/03-*` SUMMARYs, `.planning/phases/04-*` plans, and `.planning/STATE.md` are EXPLICITLY out of scope (they intentionally retain the stale phrase as audit trail).
- D-48 hook reconciliation: README hook reframed to cross-situation (NOT "on third-and-long" only).
- SHIP-08 clean: `! grep -qE '<[A-Z_]{4,}>' README.md` + same for findings/FINDINGS.md both exit 0.
- Voice/tone clean (FINDINGS.md): zero matches for AI-template phrases ("comprehensive", "leverages?", "cutting[- ]edge", "in this (article|section|memo) we will", "i (am|'m) (excited|thrilled)", "welcome to") case-insensitive.
</success_criteria>

<output>
After completion, create `.planning/phases/04-story-and-ship/04-02-SUMMARY.md` documenting:
- Final FINDINGS.md word count.
- The DET-as-team-level-beat verification result (was DET still league leader at execution time? what number landed?).
- The S3 chi-square scenario (D-13 1-5) the prose framed.
- D-49 SCOPED grep results: full stdout of the negative check (expected empty) AND the positive-presence check (one match per prose file). Note explicitly which files were IN scope (the allow-list) vs OUT of scope (.planning/phases/* and .planning/STATE.md audit trail).
- Any deviations from the locked structure (e.g., word-count overruns, table format substitutions, line-number drift on 01_exploratory.py if pattern-mapper's "line 163" was off).
- Confirmation that analysis/01_exploratory.ipynb was re-executed end-to-end after the SQL correction and that the league_anchor table now shows non-zero blitz rates (sanity that the corrected threshold produces sensible numbers).
- Confirmation that the AI-template-phrase voice grep returned zero matches in findings/FINDINGS.md.
</output>
</content>
</invoke>