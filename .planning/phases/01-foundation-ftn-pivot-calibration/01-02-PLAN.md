---
phase: 01-foundation-ftn-pivot-calibration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-foundation-ftn-pivot-calibration/01"
files_modified:
  - analysis/00_data_audit.ipynb
  - audit/ftn_null_profile.csv
  - docs/ftn-schema-audit.md
  - docs/analysis-plan.md
  - SPEC.md
  - README.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05
  - AUDIT-06
  - AUDIT-07
tags:
  - ftn
  - nfl-data-py
  - schema-audit
  - pivot-calibration
  - pre-registration
  - analysis-plan

must_haves:
  truths:
    - "audit/ftn_null_profile.csv exists and shows per-column NaN rate by play_type for FTN columns across 2022-2025"
    - "docs/ftn-schema-audit.md names the 3 or 4 chosen anchor defensive dimensions with NaN-rate-aware rationale"
    - "docs/analysis-plan.md pre-registers the 4 situations from D-06 with falsifiable hypotheses (D-08)"
    - "Public GitHub repo name is locked in writing (D-09 row appended to PROJECT.md Key Decisions table)"
    - "SPEC.md 8 business questions are reframed in-place around the chosen anchor dimensions (no Cover 0-6 references remain)"
    - "README.md Hook section contains a 2-3 sentence post-pivot framing with placeholder slots for Phase 4 numbers"
    - ".planning/PROJECT.md pandas pin matches the locked SUMMARY.md / PATTERNS.md value (>=2.1,<2.3); the stale >=2.2,<2.4 reference is removed (Pitfall #20 prevention)"
  artifacts:
    - path: "analysis/00_data_audit.ipynb"
      provides: "Recruiter-visible Phase 1 audit notebook (FEATURES.md P2: 'shows pivot reasoning visibly')"
      contains: "import nfl_data_py"
    - path: "audit/ftn_null_profile.csv"
      provides: "Per-column NaN rate by play_type, the AUDIT-02 deliverable"
      header: "column,play_type,nan_rate (or transposed equivalent)"
    - path: "docs/ftn-schema-audit.md"
      provides: "Analytical narrative confirming public-FTN reality, naming 3-4 anchors with D-01..D-04 reasoning"
      min_lines: 60
    - path: "docs/analysis-plan.md"
      provides: "Pre-registered 4-situation slate with falsifiable hypotheses (D-05..D-08 firewall)"
      min_lines: 40
    - path: "SPEC.md"
      provides: "In-place rewrite: 8 business questions reframed to chosen anchor dimensions; no 'coverage'/'Cover 0-6'/'man-zone' references in business-question section"
    - path: "README.md"
      provides: "Hook section filled with 2-3 sentence post-pivot framing; placeholder slots for headline numbers (Phase 4 fills)"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table appended with D-09 row locking public repo name; stale pandas pin corrected from >=2.2,<2.4 to >=2.1,<2.3 to match locked SUMMARY.md / requirements.txt"
      contains: "nfl-defensive-tendencies"
  key_links:
    - from: "analysis/00_data_audit.ipynb"
      to: "audit/ftn_null_profile.csv"
      via: "groupby(play_type).apply(isna).mean() → to_csv"
      pattern: "to_csv.*ftn_null_profile"
    - from: "audit/ftn_null_profile.csv"
      to: "docs/ftn-schema-audit.md (anchor selection)"
      via: "30% NaN cutoff (D-02) applied to 8 candidate columns; survivors curated for cross-category breadth (D-01)"
      pattern: "30%|0\\.30|n_blitzers|n_pass_rushers"
    - from: "docs/ftn-schema-audit.md (anchor list)"
      to: "SPEC.md (rewritten business questions)"
      via: "AUDIT-06 reframes Q1-8 around the anchors named in ftn-schema-audit.md"
      pattern: "n_blitzers|n_pass_rushers|is_play_action|is_screen_pass|is_rpo|qb_location|n_offense_backfield|starting_hash"
    - from: "docs/analysis-plan.md (4 situations)"
      to: "competitive_plays view (Phase 2)"
      via: "Each situation's filter clause is a SQL predicate the Phase 3 queries will apply on top of competitive_plays"
      pattern: "down = 3|yardline_100 <= 20|down = 1|down = 2"
    - from: "FTN ↔ pbp join in audit notebook"
      to: "Phase 2 ETL"
      via: "Establishes the validate='one_to_one' + match-rate >0.95 pattern that ETL-04 inherits"
      pattern: "validate='one_to_one'"
    - from: ".planning/PROJECT.md (corrected pandas pin)"
      to: "requirements.txt (Plan 01-01 / BOOT-02) + .planning/research/SUMMARY.md Consolidated Stack"
      via: "Single source of truth: pandas>=2.1,<2.3 across all three artifacts (Pitfall #20 doc-vs-code drift prevention)"
      pattern: "pandas>=2\\.1,<2\\.3"
---

<objective>
Run the FTN pivot calibration on the verified Python 3.11 / `nfl_data_py==0.3.3` venv from Plan 01-01: pull the 28-column FTN charting frame for 2022–2024, measure per-column NaN rates by `play_type`, apply the D-02 30%-NaN cutoff to the 8 candidate defensive dimensions, pick 3–4 anchor dimensions per D-01..D-04, pre-register the 4 analytical situations from D-06 with falsifiable hypotheses (D-08), lock the public GitHub repo name (D-09), reframe SPEC.md's 8 business questions in place, and fill the README Hook with the post-pivot framing.

Purpose: Close the eight Phase-1 open questions in `.planning/research/SUMMARY.md` "Open Questions Phase 1 Must Answer" §. Every downstream phase (ETL, schema, queries, notebooks, FINDINGS.md, ship) reads from the artifacts this plan produces — without them, Phase 2 ETL is built on assumed columns and FINDINGS.md inherits the multiple-comparisons trap.

Output: A recruiter-visible audit notebook (kept per FEATURES.md P2), a CSV of NaN rates, a written narrative naming the chosen anchors, a pre-registered 4-situation analysis plan, the locked public repo name (D-09 appended to PROJECT.md), the SPEC rewritten in place, and a post-pivot README hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md
@.planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md
@.planning/phases/01-foundation-ftn-pivot-calibration/01-01-SUMMARY.md
@CLAUDE.md
@SPEC.md
@README.md

<interfaces>
<!-- The 8 candidate FTN columns split by category (CONTEXT.md §D-01..D-04) -->

Pressure / front (measured on play_type='pass'):
  - n_blitzers
  - n_pass_rushers

Play-fakery (measured on play_type='pass'):
  - is_play_action
  - is_screen_pass
  - is_rpo

Personnel / location (measured on competitive plays = play_type IN ('pass','run')):
  - qb_location
  - n_offense_backfield
  - starting_hash

<!-- The 4 pre-registered situations (CONTEXT.md §D-06) -->

| ID | Name | Filter (SQL predicates on competitive_plays) |
|----|------|----------------------------------------------|
| S1 | 3rd-and-long | down = 3 AND ydstogo >= 7 |
| S2 | Red zone | yardline_100 <= 20 |
| S3 | 1st-and-10 | down = 1 AND ydstogo = 10 |
| S4 | 2nd-and-medium | down = 2 AND ydstogo BETWEEN 3 AND 6 |

<!-- D-07: is_play_action is a CROSS-CUTTING modifier, NOT a 5th situation. Each situation gets a play-action vs. straight-dropback split where N permits and play_type='pass'. -->

<!-- Audit notebook structure (PATTERNS.md "analysis/00_data_audit.ipynb" pattern) -->

Cell 1 — Imports + setup (nfl_data_py, pandas, numpy, seaborn, matplotlib, pathlib)
Cell 2 — Pull FTN: nfl.import_ftn_data([2022, 2023, 2024, 2025]); print row count + column list; assert len(ftn) > 130_000 (PITFALLS #17)
Cell 3 — Pull pbp (subset cols: game_id, play_id, play_type, season); 4 seasons
Cell 4 — Join FTN ↔ pbp: left_on=['nflverse_game_id', 'nflverse_play_id'], right_on=['game_id', 'play_id'], validate='one_to_one'; assert match_rate > 0.95 (PITFALLS #3)
Cell 5 — Per-column NaN rate by play_type → null_profile DataFrame → to_csv("audit/ftn_null_profile.csv")
Cell 6 — seaborn.heatmap of null_profile (in-notebook display, no savefig in Phase 1)
Cell 7 — Apply D-02 30% NaN cutoff to the 8 candidate columns; print survivors per category; document anchor decision

<!-- D-09 PROJECT.md Key Decisions row format (PATTERNS.md ".planning/PROJECT.md Key Decisions table" pattern) -->

| D-09: Public GitHub repo name | Locked to `nfl-defensive-tendencies` after Phase 1 audit confirmed the public-FTN pivot; `coverage` framing in original repo name no longer accurate. Working folder stays `nfl-coverage-tendencies` for git history continuity. | Locked 2026-04-29 |

<!-- Falsifiable-hypothesis template (CONTEXT.md §D-08, PATTERNS.md "docs/analysis-plan.md") -->

### Situation N: <name>
**Filter:** `<sql_predicate>` on `competitive_plays` (Phase 2 view).

**Hypotheses (falsifiable):**
- H1: <prediction in numbers-first form>. **Falsified if** <observable disconfirmation>.
- H2: <prediction>. **Falsified if** <observable disconfirmation>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run FTN audit notebook → null-profile CSV → schema-audit narrative (AUDIT-01, AUDIT-02, AUDIT-03)</name>
  <files>analysis/00_data_audit.ipynb, audit/ftn_null_profile.csv, docs/ftn-schema-audit.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (AUDIT-01, AUDIT-02, AUDIT-03 — exact deliverable specs)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md (sections "analysis/00_data_audit.ipynb", "audit/ftn_null_profile.csv", "docs/ftn-schema-audit.md" — full notebook structure pattern with the 7-cell template)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md (§D-01..D-04 — anchor selection rule; §D-07 — is_play_action treatment)
    - .planning/research/PITFALLS.md (Pitfall #2 lines 36-53 — voice analog for ftn-schema-audit.md; Pitfall #3 lines 56-86 — FTN ↔ pbp join keys; Pitfall #4 lines 89-110 — NaN-by-play_type expectations; Pitfall #17 lines 419-441 — row-count assertions)
    - .planning/research/STACK.md (lines 88-93 — FTN column expectations; lines 165-175 — ruff config)
    - .planning/research/ARCHITECTURE.md (lines 184-201 — Pattern 1 loader pattern)
    - CLAUDE.md (Audience Voice section + File-Organization Rules — notebooks live in analysis/, outputs cleared before commit)
    - .planning/01-01-SUMMARY.md (verify the bootstrap venv work — confirms nfl_data_py is importable on this machine)
  </read_first>
  <action>
    Execute three serial sub-steps. Each downstream sub-step depends on the artifact from the prior one.

    **Sub-step A — Build and execute `analysis/00_data_audit.ipynb` (AUDIT-01)**

    Use `jupytext` (already in the locked stack from Plan 01-01) to author the notebook. Author as a `.py` paired representation OR write the `.ipynb` JSON directly — either is fine. The notebook MUST have 7 cells per PATTERNS.md "analysis/00_data_audit.ipynb" structure:

    Cell 1 — Imports:
    ```python
    import nfl_data_py as nfl
    import pandas as pd
    import numpy as np
    import seaborn as sns
    import matplotlib.pyplot as plt
    from pathlib import Path

    SEASONS = [2022, 2023, 2024, 2025]
    AUDIT_DIR = Path("../audit")
    AUDIT_DIR.mkdir(parents=True, exist_ok=True)
    ```

    Cell 2 — Pull FTN (per PITFALLS #17 row-count assertion):
    ```python
    ftn = nfl.import_ftn_data(SEASONS)
    print(f"FTN frame: {len(ftn):,} rows × {len(ftn.columns)} columns")
    print("Columns:", ftn.columns.tolist())
    assert len(ftn) > 130_000, f"FTN pull returned {len(ftn)} rows; expected >130k for 4 seasons"
    ```

    Cell 3 — Pull pbp subset for play_type lookup:
    ```python
    pbp = nfl.import_pbp_data(SEASONS, columns=['game_id', 'play_id', 'play_type', 'season'])
    print(f"pbp frame: {len(pbp):,} rows")
    ```

    Cell 4 — Join FTN ↔ pbp on canonical keys (per PITFALLS #3 + ETL-04 pattern):
    ```python
    joined = ftn.merge(
        pbp,
        left_on=['nflverse_game_id', 'nflverse_play_id'],
        right_on=['game_id', 'play_id'],
        how='left',
        validate='one_to_one',
    )
    match_rate = joined['play_type'].notna().mean()
    print(f"FTN ↔ pbp match rate: {match_rate:.4f}")
    assert match_rate > 0.95, f"Match rate {match_rate:.3f} below 0.95 threshold"
    ```

    Cell 5 — Per-column NaN rate by `play_type` (the AUDIT-02 deliverable):
    ```python
    null_profile = (
        joined.groupby('play_type', dropna=False)
        .apply(lambda g: g.isna().mean())
        .T  # rows = columns, cols = play_type
        .round(3)
    )
    null_profile.to_csv(AUDIT_DIR / "ftn_null_profile.csv")
    print(null_profile)
    ```

    Cell 6 — Visualize NaN rate (in-notebook display only, no savefig):
    ```python
    fig, ax = plt.subplots(figsize=(10, 8))
    sns.heatmap(null_profile, annot=True, cmap='Reds', vmin=0, vmax=1, ax=ax, cbar_kws={'label': 'NaN rate'})
    ax.set_title("FTN per-column NaN rate by play_type, 2022–2024")
    fig.tight_layout()
    plt.show()
    ```

    Cell 7 — Apply 30% NaN cutoff (D-02), document survivors per category (D-01):
    ```python
    candidate_cols = {
        'pressure': ['n_blitzers', 'n_pass_rushers'],
        'play_fakery': ['is_play_action', 'is_screen_pass', 'is_rpo'],
        'personnel_location': ['qb_location', 'n_offense_backfield', 'starting_hash'],
    }

    pass_nan = null_profile.get('pass') if 'pass' in null_profile.columns else null_profile.iloc[:, 0]
    competitive_nan = (
        joined[joined['play_type'].isin(['pass', 'run'])]
        .isna()
        .mean()
        .round(3)
    )

    print("Survivors of D-02 30% cutoff (per category):")
    for category, cols in candidate_cols.items():
        for col in cols:
            if category in ('pressure', 'play_fakery'):
                rate = pass_nan.get(col, np.nan)
                scope = "play_type='pass'"
            else:
                rate = competitive_nan.get(col, np.nan)
                scope = "competitive plays"
            survives = rate <= 0.30
            print(f"  [{category}] {col} ({scope}): {rate:.3f} {'SURVIVES' if survives else 'DROPPED (>30%)'}")
    ```

    Execute the notebook end-to-end on a fresh kernel using the venv from Plan 01-01. The notebook MUST run without errors. After execution, **clear outputs before commit** (per VIZ-04 / CLAUDE.md File-Organization Rules):
    ```bash
    jupyter nbconvert --clear-output --inplace analysis/00_data_audit.ipynb
    ```
    The CSV at `audit/ftn_null_profile.csv` survives the clear (it's not a notebook output, it's a side-effect file).

    **Sub-step B — Verify `audit/ftn_null_profile.csv` (AUDIT-02)**

    Inspect the CSV to confirm schema. Per PATTERNS.md "audit/ftn_null_profile.csv":
    - Rows: every column in the FTN frame (~28 columns + the joined pbp columns).
    - Columns: one per `play_type` value (pass, run, qb_kneel, qb_spike, no_play; possibly NaN as a play_type if any join misses).
    - Cells: NaN rate, rounded to 3 decimal places, range [0.000, 1.000].

    No additional file edits — the CSV was written in Sub-step A Cell 5. Verify it exists and has a header row.

    **Sub-step C — Write `docs/ftn-schema-audit.md` (AUDIT-03)**

    Create the analytical narrative confirming the public-FTN reality and naming the chosen anchor dimensions. Per PATTERNS.md "docs/ftn-schema-audit.md" (voice analog: PITFALLS.md Pitfall 2 lines 36-53), CONTEXT.md §D-01..D-04, and CLAUDE.md Audience Voice (memo style, numbers first).

    Required content (in order):

    1. **Headline finding** (first paragraph, names the FTN-no-coverage-labels reality up front, modeled on PITFALLS.md Pitfall 2 lines 36-37):
       > The 28 columns of the public FTN charting subset distributed via nflverse contain zero coverage labels. The full Cover-0-through-6 / man/zone taxonomy is part of FTN's paid product (`ftnfantasy.com/data`), not the CC-BY-SA subset. This audit confirms the project's pivot to broader defensive tendencies — pressure, play-fakery, and personnel/location — using the dimensions FTN does expose.

    2. **The 28-column FTN inventory** (printed verbatim from `df.columns.tolist()` in the notebook).

    3. **Selection rule (D-02):** "Drop any FTN column with >30% NaN on the relevant `play_type`. Among survivors, curate for cross-category diversity across pressure / play-fakery / personnel-location (D-01)."

    4. **NaN-rate table for the 8 candidates** (markdown table, copied from notebook Cell 7 output):

       | Category | Column | Scope | NaN rate | Survives D-02 (≤30%)? |
       |---|---|---|---|---|
       | Pressure | n_blitzers | play_type='pass' | <rate> | yes/no |
       | ... (8 rows) | | | | |

    5. **Anchor selection (D-03 — aim 4, accept 3 if NaN cuts one):** Name the 3 or 4 chosen anchors, one per category where possible. Each anchor gets a 2-sentence rationale: (a) the mechanical NaN result (it survived 30%), and (b) the editorial cross-category breadth justification (D-01 story tiebreaker).

    6. **Empty-category contingency narrative (D-04):** If any category has zero survivors after the 30% cut, write a paragraph naming the empty category as a known limitation and explain that the 4th anchor came from whichever category had surplus survivors. If all 3 categories have survivors, write: "All three categories have survivors at the 30% cutoff; D-04 contingency does not apply."

    7. **Cross-cutting modifier note (D-07):** "Per D-07, `is_play_action` is a cross-cutting modifier across the 4 pre-registered situations (see `docs/analysis-plan.md`), not a separate anchor. It is included in the chosen anchors only if it survives the 30% gate AND fits the cross-category breadth curation; otherwise it is used purely as a stratifier on `play_type='pass'` rows."

    8. **Optional polish (CONTEXT.md §Claude's Discretion bullet 4):** If notebook Sub-step A surfaced material season-by-season differences in NaN rates, add a "Seasonal stability" subsection. Otherwise omit.

    **Voice rules** (CLAUDE.md lines 99-107 + Shared Pattern 1 in PATTERNS.md):
    - No exclamation points.
    - No emoji headers.
    - Numbers first ("31.7% NaN on pass plays", not "high NaN rate").
    - Memo style; reader is a peer (data-analyst recruiter or sports-analytics hiring manager).
    - State sample size or NaN rate inline with every claim.

    Minimum length: 60 lines (the file must be substantive enough to read as a real audit, not a stub).
  </action>
  <verify>
    <automated>test -f analysis/00_data_audit.ipynb && test -f audit/ftn_null_profile.csv && test -f docs/ftn-schema-audit.md && grep -q "import nfl_data_py" analysis/00_data_audit.ipynb && grep -q "import_ftn_data" analysis/00_data_audit.ipynb && grep -q "validate='one_to_one'" analysis/00_data_audit.ipynb && grep -q "match_rate > 0.95" analysis/00_data_audit.ipynb && grep -q "ftn_null_profile.csv" analysis/00_data_audit.ipynb && head -1 audit/ftn_null_profile.csv | grep -qiE "play_type|pass|run" && [ "$(wc -l < docs/ftn-schema-audit.md)" -ge 60 ] && grep -q "28" docs/ftn-schema-audit.md && grep -qiE "30%|0\.30|30 percent" docs/ftn-schema-audit.md && grep -qE "n_blitzers|n_pass_rushers|is_play_action|is_screen_pass|is_rpo|qb_location|n_offense_backfield|starting_hash" docs/ftn-schema-audit.md && ! grep -q '!' docs/ftn-schema-audit.md && [ "$(jupyter nbconvert --to script --stdout analysis/00_data_audit.ipynb 2>/dev/null | grep -c '"output_type"')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `analysis/00_data_audit.ipynb` exists and contains the literal strings `import nfl_data_py`, `import_ftn_data`, `nflverse_game_id`, `nflverse_play_id`, `validate='one_to_one'`, `match_rate > 0.95`, `ftn_null_profile.csv`
    - `analysis/00_data_audit.ipynb` outputs are CLEARED (no `"output_type"` JSON keys present in the .ipynb file — verified via `jupyter nbconvert --clear-output --inplace` having been run)
    - `audit/ftn_null_profile.csv` exists, has a header row, and the header references `play_type` or one of the play_type values (`pass`, `run`)
    - `audit/ftn_null_profile.csv` data values are numeric in [0.000, 1.000] range (NaN rates)
    - `docs/ftn-schema-audit.md` exists and is at least 60 lines (substantive, not a stub)
    - `docs/ftn-schema-audit.md` mentions the number `28` (FTN column count) and the `30%` cutoff
    - `docs/ftn-schema-audit.md` references at least 3 of the 8 candidate column names (`n_blitzers`, `n_pass_rushers`, `is_play_action`, `is_screen_pass`, `is_rpo`, `qb_location`, `n_offense_backfield`, `starting_hash`)
    - `docs/ftn-schema-audit.md` contains ZERO exclamation points (voice rule)
    - `docs/ftn-schema-audit.md` does NOT use the strings `Cover 3`, `Cover 0`, `Cover 1`, `Cover 2`, `Cover 4`, `Cover 6`, `man-zone`, or `man/zone` as ASSERTED columns we use (these may appear in prose explaining what FTN does NOT have, but never as analytical anchors; rg-check: any line starting with the words "anchor" or "we use" must NOT contain "Cover" or "man-zone")
    - The audit notebook ran successfully end-to-end (proof: `audit/ftn_null_profile.csv` was written; if the notebook had failed, this side-effect file wouldn't exist)
  </acceptance_criteria>
  <done>
    The audit notebook ran on the verified Python 3.11 venv from Plan 01-01, pulled 4 seasons of FTN charting data, joined to pbp on the canonical keys with match_rate > 0.95, wrote per-column NaN rates by play_type to `audit/ftn_null_profile.csv`, and the schema-audit narrative names the 3 or 4 chosen anchors with NaN-rate-aware reasoning. Notebook outputs are cleared on disk; the CSV is the persistent artifact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-register analysis plan (AUDIT-04)</name>
  <files>docs/analysis-plan.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (AUDIT-04 — exact deliverable spec)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md (section "docs/analysis-plan.md" — falsifiable-hypothesis template + structure)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md (§D-05 — 4-situation slate locked; §D-06 — the 4 specific situations with SQL filters; §D-07 — is_play_action as cross-cutting modifier; §D-08 — falsifiable-hypothesis format; §Specifics bullet 1 — "hypothesis wording must be falsifiable")
    - .planning/research/PITFALLS.md (Pitfall #9 lines 217-237 — multiple-comparisons trap pattern + pre-registration as the prevention)
    - docs/ftn-schema-audit.md (the chosen anchor dimensions from Task 1 — these inform which dimensions each hypothesis can claim)
    - CLAUDE.md (Audience Voice — memo style, numbers first, no exclamation points)
  </read_first>
  <action>
    Write `docs/analysis-plan.md` — the pre-registered situation slate that gates the FINDINGS.md headline-insight firewall. Per PATTERNS.md "docs/analysis-plan.md" pattern + CONTEXT.md §D-05..D-08.

    **CRITICAL voice rule (CONTEXT.md §Specifics bullet 1 + D-08):** Every hypothesis MUST be worded as a falsifiable prediction with an explicit disconfirmation clause. Example:
    - GOOD: "Defenses blitz at >35% league-wide on 3rd-and-long. **Falsified if** observed league rate ≤ 35% over 2022–2024."
    - BAD: "We will look at blitz rates on 3rd-and-long." (no prediction, no falsification clause)

    **Required structure** (write the file in this order):

    1. **Title + purpose** (2-3 sentences):
       ```markdown
       # Pre-registered Analysis Plan

       This document locks the situational slate before any Phase 3 query runs. Anything outside the 4 situations below stays in `01_exploratory.ipynb` and never appears as a headline insight in `findings/FINDINGS.md`. The strict firewall prevents the multiple-comparisons trap (PITFALLS.md Pitfall #9): scanning 32 teams × N situations and reporting "extreme" cells without correction is data dredging, not analysis.
       ```

    2. **Anchor dimensions** (1 sentence + reference to AUDIT-03):
       ```markdown
       ## Anchor Dimensions

       Each hypothesis claims findings on one of the anchor defensive dimensions named in `docs/ftn-schema-audit.md`. The cross-cutting `is_play_action` modifier (D-07) is applied as a stratifier within each situation on `play_type='pass'` rows when N permits.
       ```

    3. **The 4 pre-registered situations** (one H2 per situation, each with the falsifiable-hypothesis block from PATTERNS.md template). The 4 situations come verbatim from CONTEXT.md §D-06:

       ### Situation 1: 3rd-and-long
       **Filter:** `down = 3 AND ydstogo >= 7` on `competitive_plays` (Phase 2 view).
       **Rationale:** Pressure / blitz showcase, high recruiter recognition (D-06).
       **Hypotheses (falsifiable):**
       - H1: League-wide blitz rate (`n_blitzers > 4`) on 3rd-and-long exceeds 35%. **Falsified if** observed league rate ≤ 35% over 2022–2024 with N ≥ 1,000.
       - H2: At least 3 teams have a blitz rate > 50% on 3rd-and-long with N ≥ 100. **Falsified if** fewer than 3 teams meet both thresholds.

       ### Situation 2: Red zone
       **Filter:** `yardline_100 <= 20` on `competitive_plays`.
       **Rationale:** Tendency contraction, easy to interpret (D-06).
       **Hypotheses (falsifiable):**
       - H1: Pressure rate (`n_pass_rushers >= 5`) inside the 20 differs from the league-wide pressure rate by at least 5 percentage points (in either direction). **Falsified if** the gap is < 5pp.
       - H2: At least 1 team's red-zone tendency on the chosen anchor is "extreme" (>75% one look) with N ≥ 100. **Falsified if** no team meets both thresholds.

       ### Situation 3: 1st-and-10
       **Filter:** `down = 1 AND ydstogo = 10` on `competitive_plays`.
       **Rationale:** Neutral baseline, highest sample size, anchors league-deviation comparators (D-06).
       **Hypotheses (falsifiable):**
       - H1: League-wide pass-rate on 1st-and-10 is between 50% and 60% over 2022–2024. **Falsified if** observed rate is outside [50%, 60%].
       - H2: 1st-and-10 has the highest sample size of the 4 pre-registered situations (i.e., N(S3) > max(N(S1), N(S2), N(S4))). **Falsified if** any other situation has more competitive plays.

       ### Situation 4: 2nd-and-medium
       **Filter:** `down = 2 AND ydstogo BETWEEN 3 AND 6` on `competitive_plays`.
       **Rationale:** Strongest team-by-team variance, richest signal for the predictability index (D-06).
       **Hypotheses (falsifiable):**
       - H1: Team-level standard deviation of the chosen anchor's rate on 2nd-and-medium exceeds the team-level standard deviation on 1st-and-10. **Falsified if** σ(S4) ≤ σ(S3).
       - H2: At least 5 teams have N ≥ 100 plays on 2nd-and-medium across 2022–2024. **Falsified if** fewer than 5 teams meet the threshold.

       **NOTE:** The hypothesis numbers above are templates. The actual H1/H2 content depends on which anchors `docs/ftn-schema-audit.md` named — for each situation, write 1-2 hypotheses worded around the chosen anchor dimensions. If the chosen anchors don't include `n_blitzers`, replace H1 of Situation 1 with the equivalent hypothesis on the chosen pressure anchor (e.g., `n_pass_rushers >= 5` if that's the survivor).

    4. **Cross-cutting `is_play_action` treatment** (D-07):
       ```markdown
       ## Cross-cutting Modifier: is_play_action

       Per D-07, `is_play_action` is applied as a stratifier within each situation on `play_type='pass'` rows where N permits. For each of the 4 situations, the Phase 3 analysis produces a play-action vs. straight-dropback split; the split is reported only when both stratum sizes meet the N≥30 baseline (PITFALLS.md Pitfall #8 + STAT-07). This integrates play-fakery analysis into every claim without inflating the slate.
       ```

    5. **Firewall + multiple-comparisons discipline** (D-05 + PITFALLS.md Pitfall #9):
       ```markdown
       ## Firewall

       FINDINGS.md headline insights MUST come from this 4-situation slate. Exploratory work — including any scan across additional situations — stays in `analysis/01_exploratory.ipynb` and is labeled "Exploratory; not a headline finding." Bonferroni / BH correction is not applied because the slate is locked at 4 with one chi-square per situation; correction becomes mandatory only if the slate expands beyond pre-registered situations (which the firewall prevents).
       ```

    6. **Sample-size discipline reference** (links to STAT-07 / Phase 3 + the tiered N thresholds from CLAUDE.md "Project Realities to Remember"):
       ```markdown
       ## Sample-size discipline

       Every claim in FINDINGS.md states N inline. Tiered thresholds (per CLAUDE.md "Project Realities to Remember"):
       - **N ≥ 30** for any tendency claim.
       - **N ≥ 100** for any "extreme" claim (>75% one look).
       - **N ≥ 15** allowed only with explicit narrative low-N flag.

       Implementation: `analysis/_common.py:min_n_filter()` (Phase 3 / STAT-01) is applied to every analytical claim.
       ```

    **Voice rules** (CLAUDE.md lines 99-107):
    - No exclamation points.
    - No emoji headers.
    - Memo style, peer-to-peer, numbers first.
    - State sample size or threshold inline with every claim.

    Minimum length: 40 lines.
  </action>
  <verify>
    <automated>test -f docs/analysis-plan.md && [ "$(wc -l < docs/analysis-plan.md)" -ge 40 ] && grep -q "down = 3 AND ydstogo >= 7" docs/analysis-plan.md && grep -q "yardline_100 <= 20" docs/analysis-plan.md && grep -q "down = 1 AND ydstogo = 10" docs/analysis-plan.md && grep -q "down = 2 AND ydstogo BETWEEN 3 AND 6" docs/analysis-plan.md && [ "$(grep -c -i 'falsified if' docs/analysis-plan.md)" -ge 4 ] && grep -qi "is_play_action" docs/analysis-plan.md && grep -qi "competitive_plays" docs/analysis-plan.md && grep -qiE "N ?[≥>=]+ ?30|N ?[≥>=]+ ?100|N ?[≥>=]+ ?15" docs/analysis-plan.md && ! grep -q '!' docs/analysis-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/analysis-plan.md` exists and is at least 40 lines
    - Contains all 4 SQL filter predicates verbatim from D-06: `down = 3 AND ydstogo >= 7`, `yardline_100 <= 20`, `down = 1 AND ydstogo = 10`, `down = 2 AND ydstogo BETWEEN 3 AND 6`
    - Contains at least 4 occurrences of the phrase `falsified if` (case-insensitive) — one per hypothesis at minimum (actually ≥8 since each situation gets H1 + H2, but enforce ≥4 as the minimum)
    - Contains `is_play_action` (the D-07 cross-cutting modifier reference)
    - Contains `competitive_plays` (the Phase 2 view name that filters on top of which the situations stack)
    - Contains the tiered sample-size thresholds (`N ≥ 30`, `N ≥ 100`, `N ≥ 15` — case-insensitive, allowing `>=`)
    - Contains ZERO exclamation points
    - Does NOT contain "Cover 3", "Cover 0", "man-zone" (no false-flag references to coverage shells we don't have)
    - The H2 of every hypothesis names a quantitative threshold (a number — count, percent, or comparison)
  </acceptance_criteria>
  <done>
    The 4-situation slate is locked in writing with falsifiable hypotheses. `is_play_action` is documented as a cross-cutting modifier (D-07). The firewall is explicit: anything outside the 4 stays exploratory. Sample-size tiers (N≥30/100/15) are referenced. The file reads as a peer-to-peer pre-registration memo, not a tutorial.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock repo name + fix stale pandas pin (AUDIT-05) + rewrite SPEC business questions (AUDIT-06) + fill README hook (AUDIT-07)</name>
  <files>.planning/PROJECT.md, SPEC.md, README.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (AUDIT-05, AUDIT-06, AUDIT-07 — exact deliverable specs)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md (sections "SPEC.md (in-place rewrite per AUDIT-06)", "README.md hook rewrite (AUDIT-07)", ".planning/PROJECT.md Key Decisions table (append D-09)" — exact patterns + voice analogs)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md (§D-09 — repo name `nfl-defensive-tendencies` locked; §Claude's Discretion bullet 3 — README hook timing/content/placeholder slots)
    - .planning/PROJECT.md (existing Key Decisions table — read the full table to match formatting; D-09 row is APPENDED, not inserted; ALSO read line ~70 — the Locked tech stack line currently has stale `pandas>=2.2,<2.4` pin that contradicts requirements.txt and SUMMARY.md Consolidated Stack)
    - SPEC.md (existing 8 business questions at lines 84-94 — these are rewritten in place around the chosen anchors from `docs/ftn-schema-audit.md`)
    - README.md (the skeleton from Plan 01-01 / BOOT-06 — Hook section currently has only an HTML comment; AUDIT-07 fills it)
    - docs/ftn-schema-audit.md (the anchor dimensions chosen in Task 1 — informs the SPEC rewrite)
    - .planning/research/SUMMARY.md (line 12 — voice analog for the README hook; the post-pivot framing already exists in plain English; ALSO Consolidated Stack section — locked `pandas>=2.1,<2.3` pin, the source-of-truth value)
    - CLAUDE.md (Audience Voice + Public-Repo Discipline — Locked Stack section also names `pandas>=2.1,<2.3`)
    - .planning/research/PITFALLS.md (Pitfall #20 lines 502-521 — README↔code drift prevention; this task is the prevention step, AND PROJECT.md's stale pin is exactly this drift pattern)
  </read_first>
  <action>
    Three independent sub-edits across three files. Per PATTERNS.md they share the same voice analog (CLAUDE.md Audience Voice) but touch different files; they can run in any order within this task.

    **Sub-step A — Append D-09 to `.planning/PROJECT.md` Key Decisions table (AUDIT-05) AND fix the stale pandas pin (Pitfall #20 prevention)**

    This sub-step makes TWO edits to `.planning/PROJECT.md`:

    **(A1) Append D-09 row to the Key Decisions table.** Read the existing Key Decisions table (around lines 78-95 of PROJECT.md). Match the exact column count and pipe alignment of the existing rows. Append a new row at the end of the table (before the closing line of the section, after the existing rows):

    ```markdown
    | D-09: Public GitHub repo name | Locked to `nfl-defensive-tendencies` after Phase 1 audit confirmed the public-FTN pivot; `coverage` framing in original repo name no longer accurate. Working folder stays `nfl-coverage-tendencies` for git history continuity. | Locked 2026-04-29 |
    ```

    Per PATTERNS.md ".planning/PROJECT.md Key Decisions table" pattern: this is an in-place table append, NOT a new ADR file (CONTEXT.md §canonical_refs explicitly forbids new ADRs). The existing table format must be preserved — count the columns in the existing first row and ensure D-09 has the same number of pipes.

    Do NOT change the working folder name. The repo name `nfl-defensive-tendencies` is used only in Phase 4 / SHIP-03 (GitHub MCP repo creation) per CONTEXT.md §D-09. The local directory stays `nfl-coverage-tendencies`.

    **(A2) Fix the stale pandas pin on line ~70 of `.planning/PROJECT.md` (Pitfall #20 doc-vs-code drift prevention).**

    Line ~70 of `.planning/PROJECT.md` currently reads (search for the exact substring):

    > `pandas>=2.2,<2.4`

    This contradicts the locked single-source-of-truth `pandas>=2.1,<2.3` value in `.planning/research/SUMMARY.md` Consolidated Stack and in `requirements.txt` (BOOT-02 / Plan 01-01). The discrepancy is exactly the doc-vs-code drift pattern PITFALLS.md Pitfall #20 prevents. Replace the stale pin in place:

    - Find: `pandas>=2.2,<2.4`
    - Replace with: `pandas>=2.1,<2.3`

    The surrounding sentence (`**Locked tech stack**: ...`) stays intact; only the pandas pin substring changes. This rewrite makes PROJECT.md, requirements.txt, SUMMARY.md, and PATTERNS.md all agree on the locked pandas pin.

    Update the `*Last updated:* 2026-04-29 ...` line at the bottom of PROJECT.md if it exists, to reflect "Phase 1 D-09 lock + pandas pin alignment".

    **Sub-step B — In-place rewrite of SPEC.md business questions (AUDIT-06)**

    Read the existing SPEC.md "Business Questions to Answer" section (lines 84-94). The 8 questions currently use coverage / man-zone / "looks" framing. Rewrite each question in place, replacing those terms with references to the anchor dimensions named in `docs/ftn-schema-audit.md`. Per PATTERNS.md "SPEC.md (in-place rewrite per AUDIT-06)" pattern + REQUIREMENTS.md AUDIT-06.

    **Rewriting rules:**
    - Preserve the section heading (`## Business Questions to Answer`) and the intro line.
    - Preserve the numbering (1 through 8) and the bold headline pattern (e.g., **Distribution baseline:**).
    - Replace "defensive tendency mix" / "tendencies" / "looks" / "coverage" with the specific anchor names chosen in AUDIT-03 (e.g., "blitz rate (`n_blitzers > 4`)", "pass-rusher count distribution", "play-action rate", "personnel-by-formation"). Use the actual anchors from `docs/ftn-schema-audit.md` — read that file first.
    - Question 6 (Play-action vulnerability): keep the top-line framing but reframe per D-07 as a cross-cutting modifier across the other 4 situations rather than its own slate item.
    - Question 8 (Exploitable matchups): reframe to honor the 4-situation pre-registration firewall (D-05) — extreme-tendency claims only come from the 4 pre-registered situations.
    - Do NOT invent new questions; do NOT delete questions. The count stays at 8.
    - Do NOT use the strings `Cover 0`, `Cover 1`, `Cover 2`, `Cover 3`, `Cover 4`, `Cover 6`, `man-zone`, `man/zone`, `coverage shell` anywhere in the rewritten questions.

    Do NOT rewrite the rest of SPEC.md (Audience, Tech Stack, Repo Structure, Out of Scope) — per PATTERNS.md, the rest is already post-pivot accurate.

    **Sub-step C — Fill README.md Hook section (AUDIT-07)**

    Read the README.md skeleton from Plan 01-01. The `## Hook` section currently has only an HTML comment placeholder. Replace that comment with a 2-3 sentence post-pivot framing per PATTERNS.md "README.md hook rewrite (AUDIT-07)" pattern + CONTEXT.md §Claude's Discretion bullet 3 (default approach: 2-3 sentences with placeholder slots for headline numbers Phase 4 fills).

    **Hook content rules:**
    1. Sentence 1: Names what the project does in numbers-first memo style. Reference the 3-season scope and the data sources. Voice analog: SUMMARY.md line 12.
    2. Sentence 2: Names the public-FTN pivot honestly — public FTN exposes broader defensive tendencies (pressure / play-fakery / personnel), NOT coverage shells (Cover 0–6, man/zone). The pivot is acknowledged, not buried.
    3. Sentence 3 (optional): Includes 1-2 placeholder slots in angle-bracket form (e.g., `<MOST_PREDICTABLE_DEFENSE>` or `<LEAGUE_BLITZ_RATE_3RD_LONG>`) that Phase 4 fills with actual headline numbers. PATTERNS.md AUDIT-07 says "with placeholder slots for headline numbers Phase 4 fills".

    **Voice rules** (CLAUDE.md lines 99-107 + PITFALLS.md Pitfall #13 — no jargon):
    - No "Cover 3", no "shell", no "DVOA" without a glossary gloss (and the glossary is empty in Phase 1; just don't use those terms).
    - No exclamation-point punctuation in rendered prose. (HTML comment delimiters `<!-- ... -->` are explicitly allowed because their `!` is comment syntax, not English punctuation. The voice rule targets exclamation-point punctuation, not the literal `!` byte.)
    - No emoji headers (the `## Hook` H2 stays unchanged from the skeleton; only the body content is filled).
    - Memo style; the non-football recruiter must parse it cold.

    **Example Hook (use as a template; adjust to your final wording):**
    ```markdown
    ## Hook

    Three seasons (2022–2024) of NFL play-by-play and FTN charting, asking which defenses are predictable in known situations. The public FTN charting subset distributed via nflverse exposes broader defensive tendencies — pressure, play-fakery, personnel and location — but not the proprietary Cover 0–6 / man-zone labels (those are part of FTN's paid product). This project pivots to those broader tendencies and ranks 32 defenses on a 0–100 predictability index — <MOST_PREDICTABLE_DEFENSE> ranks first; <MOST_PREDICTABLE_INDEX_VALUE> separates them from the league average.
    ```

    Do NOT modify any other section of README.md. The Hook is the only Phase 1 fill; the other 8 sections stay as empty headers + HTML comments per BOOT-06 (Plan 01-01).
  </action>
  <verify>
    <automated>grep -q "D-09: Public GitHub repo name" .planning/PROJECT.md && grep -q "nfl-defensive-tendencies" .planning/PROJECT.md && grep -q "Locked 2026-04-29" .planning/PROJECT.md && ! grep -q "pandas>=2.2,<2.4" .planning/PROJECT.md && grep -q "pandas>=2.1,<2.3" .planning/PROJECT.md && grep -q "## Business Questions to Answer" SPEC.md && ! grep -E "Cover 0|Cover 1|Cover 2|Cover 3|Cover 4|Cover 6|man-zone|man/zone|coverage shell" SPEC.md | grep -vi "out of scope\|paid product\|public ftn\|not in" && [ "$(grep -c -E "^[0-9]+\. " SPEC.md)" -ge 8 ] && grep -q "## Hook$" README.md && [ "$(awk '/^## Hook$/{flag=1; next} /^## [A-Z]/{flag=0} flag' README.md | wc -l)" -ge 4 ] && grep -E "<[A-Z_]+>" README.md && [ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ] && ! grep -E "Cover 3|Cover 0" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains a new row in the Key Decisions table with the literal string `D-09: Public GitHub repo name`
    - `.planning/PROJECT.md` D-09 row contains `nfl-defensive-tendencies` (the locked public repo name)
    - `.planning/PROJECT.md` D-09 row contains `Locked 2026-04-29` (the lock date in the Outcome column, matching existing row format)
    - `.planning/PROJECT.md` D-09 row preserves the table column count (3 columns: Decision | Rationale | Outcome)
    - `.planning/PROJECT.md` does NOT contain the stale string `pandas>=2.2,<2.4` anywhere — verified via `! grep -q "pandas>=2.2,<2.4" .planning/PROJECT.md` (Pitfall #20 doc-vs-code drift prevention)
    - `.planning/PROJECT.md` contains the locked string `pandas>=2.1,<2.3` — verified via `grep -q "pandas>=2.1,<2.3" .planning/PROJECT.md`. This single-source-of-truth value matches `requirements.txt` (BOOT-02) and `.planning/research/SUMMARY.md` Consolidated Stack
    - `SPEC.md` retains the heading `## Business Questions to Answer`
    - `SPEC.md` Business Questions section has 8 numbered questions (`1.` through `8.` at line starts)
    - `SPEC.md` Business Questions section does NOT contain the strings `Cover 0`, `Cover 1`, `Cover 2`, `Cover 3`, `Cover 4`, `Cover 6`, `man-zone`, `man/zone`, `coverage shell` AS ANALYTICAL CLAIMS (these strings may appear in the existing "Out of Scope" section explaining what we DON'T have — the grep guard above filters those false positives)
    - `SPEC.md` Business Questions reference at least 2 of the 8 candidate anchor columns (`n_blitzers`, `n_pass_rushers`, `is_play_action`, `is_screen_pass`, `is_rpo`, `qb_location`, `n_offense_backfield`, `starting_hash`) — the actual anchors named in `docs/ftn-schema-audit.md`
    - `README.md` `## Hook` section is no longer empty — has at least 4 lines of body content between the `## Hook` H2 and the next H2 (verified via `awk '/^## Hook$/{flag=1; next} /^## [A-Z]/{flag=0} flag' README.md | wc -l` >= 4; the awk range counts lines strictly between `## Hook` and the next H2 starting with a capital letter, fixing the broken `awk /^## Hook$/,/^## /` self-matching bug)
    - `README.md` Hook contains at least one placeholder slot in angle-bracket form (regex `<[A-Z_]+>`)
    - `README.md` does NOT contain `Cover 3`, `Cover 0`, or other coverage-shell jargon (PITFALLS.md Pitfall #13 — non-football recruiter must parse it cold)
    - `README.md` contains NO exclamation-point punctuation in rendered prose — verified by stripping HTML comments first and then grepping for `!`. HTML comment delimiters (`<!-- ... -->`) are explicitly allowed because their `!` is comment syntax, not English punctuation. Concretely: `[ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ]` (zero `!` occurrences in non-comment content)
    - The other 8 H2 sections of README.md (Findings preview, Architecture, Setup, Glossary, Methodology, Limitations, Attribution, Known Issues) are unchanged from the BOOT-06 skeleton (empty bodies + HTML comments)
  </acceptance_criteria>
  <done>
    D-09 is locked in PROJECT.md Key Decisions (no new ADR file). The stale `pandas>=2.2,<2.4` reference on PROJECT.md line ~70 is corrected to `pandas>=2.1,<2.3`, aligning all four artifacts (PROJECT.md, requirements.txt, SUMMARY.md, PATTERNS.md) on the locked pin (Pitfall #20 prevention). SPEC.md's 8 business questions are reframed in place around the anchors from `docs/ftn-schema-audit.md`; no false-flag references to Cover 0–6 / man-zone remain in the analytical sections. README.md Hook has a 2-3 sentence post-pivot framing with at least one placeholder slot for Phase 4 to fill; HTML comment delimiters are preserved (allowed exception to the no-`!` voice rule). The other README sections remain empty per BOOT-06.
  </done>
</task>

</tasks>

<verification>
Phase-level checks across all three tasks:

```bash
# AUDIT-01..03 deliverables (Task 1)
test -f analysis/00_data_audit.ipynb
test -f audit/ftn_null_profile.csv
test -f docs/ftn-schema-audit.md
grep -q "validate='one_to_one'" analysis/00_data_audit.ipynb
grep -q "match_rate > 0.95" analysis/00_data_audit.ipynb
[ "$(wc -l < docs/ftn-schema-audit.md)" -ge 60 ]

# AUDIT-04 deliverable (Task 2)
test -f docs/analysis-plan.md
[ "$(wc -l < docs/analysis-plan.md)" -ge 40 ]
[ "$(grep -c -i 'falsified if' docs/analysis-plan.md)" -ge 4 ]
grep -q "down = 3 AND ydstogo >= 7" docs/analysis-plan.md
grep -q "down = 2 AND ydstogo BETWEEN 3 AND 6" docs/analysis-plan.md

# AUDIT-05..07 deliverables (Task 3)
grep -q "D-09: Public GitHub repo name" .planning/PROJECT.md
grep -q "nfl-defensive-tendencies" .planning/PROJECT.md
# Pitfall #20: PROJECT.md pandas pin must match requirements.txt + SUMMARY.md
! grep -q "pandas>=2.2,<2.4" .planning/PROJECT.md
grep -q "pandas>=2.1,<2.3" .planning/PROJECT.md
[ "$(grep -c -E '^[0-9]+\. ' SPEC.md)" -ge 8 ]
grep -E "<[A-Z_]+>" README.md
grep -q "## Hook$" README.md
# Hook body has substantive content (>= 4 lines between ## Hook and next H2)
[ "$(awk '/^## Hook$/{flag=1; next} /^## [A-Z]/{flag=0} flag' README.md | wc -l)" -ge 4 ]

# Voice rules (HTML comment delimiters allowed; only exclamation-point punctuation in rendered prose forbidden)
[ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ]
! grep -q '!' docs/ftn-schema-audit.md
! grep -q '!' docs/analysis-plan.md

# Public-FTN reality (PITFALLS Pitfall #2 + #20)
# No analytical references to coverage shells
! grep -E "(WHERE|SELECT) .*(Cover [0-9]|man.?zone)" SPEC.md docs/*.md README.md analysis/00_data_audit.ipynb 2>/dev/null
```
</verification>

<success_criteria>
1. **AUDIT-01:** `analysis/00_data_audit.ipynb` exists, calls `nfl.import_ftn_data([2022, 2023, 2024, 2025])`, prints column inventory, computes per-column NaN rates by `play_type`, joins FTN ↔ pbp with `validate='one_to_one'` and asserts match_rate > 0.95. Notebook outputs cleared on disk.
2. **AUDIT-02:** `audit/ftn_null_profile.csv` exists with NaN rates per FTN column × play_type for 2022–2024.
3. **AUDIT-03:** `docs/ftn-schema-audit.md` (≥60 lines) names the 3 or 4 chosen anchor defensive dimensions with NaN-rate-aware rationale, applies the 30% cutoff to the 8 candidates per category, and documents D-04 contingency narrative if any category is empty.
4. **AUDIT-04:** `docs/analysis-plan.md` (≥40 lines) pre-registers the 4 D-06 situations with at least 4 falsifiable hypotheses (each with explicit "Falsified if" disconfirmation clauses), names `is_play_action` as the cross-cutting modifier (D-07), and references the firewall + tiered N thresholds.
5. **AUDIT-05:** `.planning/PROJECT.md` Key Decisions table has a D-09 row locking `nfl-defensive-tendencies` (no new ADR file created). Additionally, the stale `pandas>=2.2,<2.4` reference on PROJECT.md line ~70 is corrected to `pandas>=2.1,<2.3` to match requirements.txt and SUMMARY.md (Pitfall #20 doc-vs-code drift prevention).
6. **AUDIT-06:** `SPEC.md`'s 8 business questions are reframed in place around the chosen anchor dimensions; no analytical references to Cover 0–6 / man-zone remain (only the "Out of Scope" section explains what we DON'T have).
7. **AUDIT-07:** `README.md` Hook section contains a 2-3 sentence post-pivot framing with at least one `<PLACEHOLDER_SLOT>` for Phase 4 to fill (verified by counting body lines between `## Hook` and the next H2 with the working awk range expression); other sections remain empty per BOOT-06. HTML comment delimiters in the unchanged sections are explicitly allowed under the voice rule (which targets exclamation-point punctuation, not the literal `!` byte).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nflverse CDN → recruiter laptop (Plan 01-02 Task 1 audit notebook) | First touch of external data; FTN charting + nflfastR pbp pulled via `nfl_data_py.import_*_data()` |
| audit notebook → audit/ftn_null_profile.csv | Side-effect file write; the CSV becomes a downstream input to `docs/ftn-schema-audit.md` and `SPEC.md` rewrite |
| Phase 1 artifacts → Phase 2/3/4 (deferred) | The anchor dimensions and pre-registered situations bind every downstream phase |
| .planning docs ↔ committed code | Doc-vs-code drift boundary (Pitfall #20); Task 3 Sub-step A2 is the prevention step for the pandas pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | FTN ↔ pbp join silently mis-joining (PITFALLS #3) | mitigate | Audit notebook Cell 4 uses `validate='one_to_one'` on `nflverse_game_id` + `nflverse_play_id`; asserts match_rate > 0.95. Same pattern propagates to Phase 2 ETL-04. |
| T-01-02-02 | Information Disclosure | Notebook outputs (large dataframes, charter names) committed to git | mitigate | Task 1 runs `jupyter nbconvert --clear-output --inplace` before commit (per VIZ-04 / CLAUDE.md File-Organization). Outputs cleared on disk; only side-effect CSV persists. |
| T-01-02-03 | Repudiation | Anchor dimension choice not traceable to NaN rates | mitigate | Audit notebook Cell 5 writes the full NaN profile to CSV; `docs/ftn-schema-audit.md` cites the CSV row-by-row for each candidate column. Auditable trail. |
| T-01-02-04 | Information Disclosure | Audit notebook surfaces PII (player health data, charter identities) | accept | FTN charting + nflfastR pbp are CC-BY-SA 4.0 / MIT public datasets; no PII in the schema. Confirmed by the column inventory in AUDIT-01. |
| T-01-02-05 | Denial of Service | nflverse CDN 404 during audit pull (PITFALLS #17) | mitigate | Audit notebook Cell 2 includes `assert len(ftn) > 130_000` row-count assertion; failure surfaces as a hard error rather than silent zero-row continue. |
| T-01-02-06 | Spoofing | False "anchor selection" — anchors picked outside the D-02 30% rule | mitigate | Acceptance criteria: `docs/ftn-schema-audit.md` must cite the 30% cutoff and reference at least 3 candidate columns; the NaN-rate table is published; the cross-category breadth justification is documented per D-01. |
| T-01-02-07 | Information Disclosure | README hook leaking jargon that confuses non-football recruiters (PITFALLS #13) | mitigate | Acceptance criteria forbid "Cover 3" / "Cover 0" / coverage-shell jargon in README.md. Hook is "non-football recruiter must parse cold." |
| T-01-02-08 | Tampering | SPEC rewrite drift — analytical questions reference columns we don't have | mitigate | Acceptance criteria forbid "Cover 0–6" / "man-zone" / "coverage shell" strings in SPEC business questions. Out of Scope section retains them, but Business Questions reference only the chosen anchors from `docs/ftn-schema-audit.md`. PITFALLS #20 prevention. |
| T-01-02-09 | Elevation of Privilege | New ADR file created instead of in-place PROJECT.md append | mitigate | Task 3 Sub-step A explicitly forbids new ADR files (CONTEXT.md §canonical_refs). D-09 is appended to existing Key Decisions table only. |
| T-01-02-10 | Tampering | Stale pandas pin in PROJECT.md (`>=2.2,<2.4`) drifts from requirements.txt and SUMMARY.md (`>=2.1,<2.3`); a future maintainer reads PROJECT.md and adopts the wrong pin | mitigate | Task 3 Sub-step A2 rewrites the stale pin to `>=2.1,<2.3`; acceptance criteria include both `! grep -q "pandas>=2.2,<2.4" .planning/PROJECT.md` and `grep -q "pandas>=2.1,<2.3" .planning/PROJECT.md`. PITFALLS Pitfall #20 prevention. |
</threat_model>

<output>
After completion, create `.planning/phases/01-foundation-ftn-pivot-calibration/01-02-SUMMARY.md` documenting:
- Files created/modified (7 — 1 notebook + 1 CSV + 2 docs + 1 SPEC rewrite + 1 README hook fill + 1 PROJECT.md append+pin-fix)
- AUDIT-01..07 acceptance criteria evidence
- The 3 or 4 anchor dimensions chosen (cite from `docs/ftn-schema-audit.md`)
- Whether D-04 contingency applied (any category empty after 30% cut?)
- The 4 pre-registered situations confirmed locked (cite from `docs/analysis-plan.md`)
- D-09 PROJECT.md row diff (paste the appended row verbatim)
- PROJECT.md pandas pin diff (paste before/after of the line-~70 sentence; confirm Pitfall #20 prevention applied)
- Phase 1 closeout: confirm all 5 ROADMAP success criteria are met (BOOT install verified, NaN profile exists, anchors named, situations pre-registered, repo name + README hook locked)
- Open questions handed off to Phase 2 (FTN ↔ pbp match rate observed in audit; expected ETL-04 threshold)
</output>
