---
phase: 01-foundation-ftn-pivot-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - .python-version
  - .gitignore
  - README.md
  - data/raw/.gitkeep
  - data/README.md
  - schema/.gitkeep
  - etl/.gitkeep
  - queries/.gitkeep
  - analysis/.gitkeep
  - findings/.gitkeep
  - findings/images/.gitkeep
  - audit/.gitkeep
  - docs/.gitkeep
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
  - BOOT-05
  - BOOT-06
  - BOOT-07
tags:
  - bootstrap
  - python
  - nfl-data-py
  - portfolio
  - venv

must_haves:
  truths:
    - "A fresh Python 3.11 venv installs requirements.txt cleanly with no errors"
    - "python -c 'import nfl_data_py' exits 0 in the fresh venv"
    - "A recruiter can clone the repo and see the directory tree from .planning/research/ARCHITECTURE.md"
    - "ruff check . exits 0 (or only with E501 ignored as configured)"
    - "git status shows no .db, no parquet, no .env files staged"
  artifacts:
    - path: "requirements.txt"
      provides: "v1 LOCKED stack pin block per SUMMARY.md Consolidated Stack"
      contains: "nfl_data_py==0.3.3"
    - path: "pyproject.toml"
      provides: "[tool.ruff] config only (line-length=100, target=py311, select E/F/I/B/UP)"
      contains: "[tool.ruff]"
    - path: ".python-version"
      provides: "Python 3.11 pin for pyenv users"
      contains: "3.11"
    - path: ".gitignore"
      provides: "Public-repo discipline (.db, data/raw/, .env*, scratch/, *.log gitignored)"
      contains: "*.db"
    - path: "README.md"
      provides: "Skeleton with empty section headers (no claims yet); voice-rule-compliant"
      min_lines: 20
    - path: "data/README.md"
      provides: "Stub explaining .db regeneration via etl/run.py (full version is Phase 4 DOC-08)"
      min_lines: 5
    - path: "data/raw/.gitkeep"
      provides: "Empty placeholder so gitignored data/raw/ directory exists for Phase 2 ETL"
  key_links:
    - from: "requirements.txt"
      to: ".python-version"
      via: "Python 3.11 binds nfl_data_py==0.3.3 (3.13 install fails per upstream issue #122)"
      pattern: "3.11"
    - from: "requirements.txt"
      to: "numpy<2.0 constraint"
      via: "nfl_data_py 0.3.x references np.float_ (removed in NumPy 2.0)"
      pattern: "numpy>=1.26,<2.0"
    - from: ".gitignore"
      to: "data/*.db"
      via: "BOOT-03 lists *.db; replaces existing 'tracked decision deferred' comment"
      pattern: "\\*\\.db"
    - from: "BOOT-07 install verification"
      to: "All other BOOT-* deliverables"
      via: "Single serial gate at end of Wave 1; downstream 01-02 needs working venv"
      pattern: "import nfl_data_py"
---

<objective>
Bootstrap the NFL Defensive Tendencies repo skeleton: pinned `requirements.txt`, `pyproject.toml` (ruff config only), `.python-version`, expanded `.gitignore`, voice-compliant README skeleton, full directory tree per `.planning/research/ARCHITECTURE.md`, and a fresh-venv install verification that proves `import nfl_data_py` works.

Purpose: Establish the foundation every downstream phase builds on. BOOT-07 is the single serial gate that proves the locked stack actually installs on a clean Python 3.11 venv before Plan 01-02 runs the FTN audit notebook.

Output: A clean, lintable, gitignored-correctly repo with the verified Python 3.11 + `nfl_data_py==0.3.3` + `numpy<2.0` stack ready for the Phase 1 audit and all of Phases 2–4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md
@.planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md
@CLAUDE.md
@.gitignore

<interfaces>
<!-- Verbatim pin block from .planning/research/SUMMARY.md lines 67-91 (PATTERNS.md transcribes it) -->
<!-- This is THE ONLY valid version block for requirements.txt. Do NOT use STACK.md's `>=2.2,<2.4` pandas range — PATTERNS.md flags that drift. -->

requirements.txt content (verbatim, including comments):
```text
# requirements.txt — v1 LOCKED
# Data layer (archived upstream; intentional pin — see README "Known Issues")
nfl_data_py==0.3.3

# Numerical / data
pandas>=2.1,<2.3
numpy>=1.26,<2.0          # forced — nfl_data_py 0.3.x uses np.float_, removed in NumPy 2.0
pyarrow>=15,<22           # parquet I/O; required transitively by nfl_data_py

# Statistical
scipy>=1.13,<1.18         # chi2_contingency + entropy

# Visualization
matplotlib>=3.8,<3.11
seaborn==0.13.2           # exact pin — only release in 0.13.x line

# Notebooks
jupyterlab>=4.2,<4.6
ipykernel>=6.29,<8
jupytext>=1.16,<2         # paired .py representation for clean git diffs
nbconvert>=7.16,<8        # for one-time clear-output before commit

# Dev tooling
ruff>=0.6,<1.0
```

pyproject.toml content (verbatim, from .planning/research/STACK.md lines 165-175):
```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]   # pycodestyle, pyflakes, isort, bugbear, pyupgrade
ignore = ["E501"]                      # let line-length do that work; long URL strings are fine
```

.python-version content (literal, single line + trailing newline):
```
3.11
```

Directory tree to create (per .planning/research/ARCHITECTURE.md lines 102-153):
- data/, data/raw/ (with .gitkeep), schema/, etl/, queries/, analysis/, findings/, findings/images/, audit/, docs/

Voice rules for README skeleton (CLAUDE.md lines 99-107 + PATTERNS.md "Skeleton section list"):
- Memo style. No exclamation-point punctuation. No emoji section headers. No "Welcome to my project!".
- HTML comment delimiters (`<!-- ... -->`) are explicitly ALLOWED — they contain `!` characters as part of comment syntax, not as English punctuation. The voice rule targets exclamation-point punctuation, not the literal `!` byte.
- Numbers first when claims appear (Phase 1 has no claims yet — only empty headers + HTML comment placeholders).
- README sections (per PATTERNS.md "Skeleton section list" pattern):
  Hook → Findings preview → Architecture → Setup → Glossary → Methodology → Limitations → Attribution → Known Issues
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pinned config files (requirements.txt, pyproject.toml, .python-version) and expand .gitignore</name>
  <files>requirements.txt, pyproject.toml, .python-version, .gitignore</files>
  <read_first>
    - .planning/research/SUMMARY.md (lines 66-91 — verbatim Consolidated Stack pin block)
    - .planning/research/STACK.md (lines 165-175 — verbatim ruff config block)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md (sections "requirements.txt", "pyproject.toml", ".python-version", ".gitignore" — pattern assignments + drift warnings)
    - .planning/REQUIREMENTS.md (BOOT-02, BOOT-03, BOOT-04, BOOT-05 — exact pin acceptance criteria)
    - .gitignore (existing 31-line file — must be expanded in place, not replaced)
    - CLAUDE.md (Locked Stack section — Python 3.11 / nfl_data_py 0.3.3 / numpy<2 / pandas>=2.1,<2.3 non-negotiables)
    - .planning/research/PITFALLS.md (Pitfall #14 lines 336-358 — `.db` MUST be hard-gitignored, not deferred)
  </read_first>
  <action>
    Create four config files in parallel (all are independent static manifests with no inter-file dependencies):

    **(a) `requirements.txt`** — Write the 14-package pin block VERBATIM from `.planning/research/SUMMARY.md` lines 67-91. Implements BOOT-02. Per PATTERNS.md "requirements.txt" pattern: pandas pin is `>=2.1,<2.3` (NOT STACK.md's `>=2.2,<2.4`; PATTERNS.md explicitly flags this drift). Per D-01..D-10 reading, no nflreadpy line. Include comments verbatim (e.g., `# forced — nfl_data_py 0.3.x uses np.float_, removed in NumPy 2.0`).

    **(b) `pyproject.toml`** — Write the `[tool.ruff]` + `[tool.ruff.lint]` blocks VERBATIM from `.planning/research/STACK.md` lines 166-175. Implements BOOT-04. NO `[project]`, NO `[build-system]`, NO package-metadata sections — this is an analysis repo, not a library. `requirements.txt` is the install manifest.

    **(c) `.python-version`** — Single line `3.11` followed by a trailing newline. No other content. Implements BOOT-05.

    **(d) `.gitignore`** — Expand the existing 31-line file in place (read first, do NOT replace from scratch — preserve existing entries). Per PATTERNS.md ".gitignore" pattern + PITFALLS.md Pitfall #14 + #18:
      1. ADD a hard `*.db` line (under the "# Data" section).
      2. REPLACE the comment line `# Note: data/nfl_coverage.db tracked decision deferred to ship phase (size-dependent)` — that decision is now LOCKED to gitignored.
      3. REPLACE `.env` and `.env.local` literal lines with a single `.env*` glob (catches `.env.production`, `.env.foo`, etc.). Keep `*.key` and `*.pem`.
      4. ADD a "# Scratch / temp" section with `scratch/`, `scratch*`, `tmp_*`, `*.log`.
      5. CONFIRM `data/raw/`, `__pycache__/`, `.ipynb_checkpoints/`, `venv/`, `.venv/` remain (they exist).
      Implements BOOT-03.
  </action>
  <verify>
    <automated>test -f requirements.txt && test -f pyproject.toml && test -f .python-version && test -f .gitignore && grep -q "^nfl_data_py==0.3.3$" requirements.txt && grep -q "^numpy>=1.26,<2.0" requirements.txt && grep -q "^pandas>=2.1,<2.3$" requirements.txt && grep -q "^seaborn==0.13.2" requirements.txt && grep -q "^pyarrow>=15,<22" requirements.txt && grep -q "^ruff>=0.6,<1.0" requirements.txt && grep -q '^\[tool\.ruff\]$' pyproject.toml && grep -q 'target-version = "py311"' pyproject.toml && grep -q 'line-length = 100' pyproject.toml && grep -q 'select = \["E", "F", "I", "B", "UP"\]' pyproject.toml && [ "$(cat .python-version | tr -d '[:space:]')" = "3.11" ] && grep -q "^\*\.db$" .gitignore && grep -q "^\.env\*$" .gitignore && grep -q "^scratch/$" .gitignore && grep -q "^data/raw/$" .gitignore && ! grep -q "^\.env$" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` exists and contains the literal line `nfl_data_py==0.3.3` (exact pin, no version range)
    - `requirements.txt` contains `numpy>=1.26,<2.0` (the BOOT-02 forced upper bound — `<2.0`, NOT `<2.3`)
    - `requirements.txt` contains `pandas>=2.1,<2.3` (the PATTERNS.md-corrected band, NOT STACK.md's `>=2.2,<2.4`)
    - `requirements.txt` contains `seaborn==0.13.2` (exact pin per BOOT-02)
    - `requirements.txt` contains `pyarrow>=15,<22`, `scipy>=1.13,<1.18`, `matplotlib>=3.8,<3.11`, `jupyterlab>=4.2,<4.6`, `ipykernel>=6.29,<8`, `jupytext>=1.16,<2`, `nbconvert>=7.16,<8`, `ruff>=0.6,<1.0`
    - `pyproject.toml` contains `[tool.ruff]` and `target-version = "py311"` and `line-length = 100`
    - `pyproject.toml` contains `select = ["E", "F", "I", "B", "UP"]` and `ignore = ["E501"]`
    - `pyproject.toml` does NOT contain `[project]` or `[build-system]` sections (this is an analysis repo)
    - `.python-version` contains exactly `3.11` (after trimming whitespace)
    - `.gitignore` contains a literal `*.db` line on its own (no leading characters)
    - `.gitignore` contains `.env*` (glob form) and does NOT contain a bare `.env` line (replaced by glob)
    - `.gitignore` contains `scratch/`, `data/raw/`, `__pycache__/`, `.ipynb_checkpoints/`, `venv/`
    - `.gitignore` does NOT contain the deferral comment `# Note: data/nfl_coverage.db tracked decision deferred to ship phase` (replaced by hard `*.db` rule per Pitfall #14)
  </acceptance_criteria>
  <done>
    All four config files exist with the BOOT-02..05 acceptance criteria met. `pandas>=2.1,<2.3` is the locked pin (not STACK.md's `>=2.2,<2.4`). `.db` is a hard gitignore rule. No bare `.env` line — the glob `.env*` replaces it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create directory tree skeleton + README skeleton + data/README.md stub</name>
  <files>data/raw/.gitkeep, data/README.md, schema/.gitkeep, etl/.gitkeep, queries/.gitkeep, analysis/.gitkeep, findings/.gitkeep, findings/images/.gitkeep, audit/.gitkeep, docs/.gitkeep, README.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (lines 102-153 — canonical directory tree)
    - .planning/research/SUMMARY.md (lines 165-220 — Consolidated Architecture Sketch + lines 142-156 P3 Anti-Features)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-PATTERNS.md (sections "Repo skeleton directories", "README.md (skeleton, BOOT-06)", "Shared Pattern 1: Audience voice", "Shared Pattern 4: File-organization discipline")
    - .planning/REQUIREMENTS.md (BOOT-01, BOOT-06 — directory tree + README skeleton acceptance criteria)
    - CLAUDE.md (lines 36-58 Project Structure + lines 99-107 Audience Voice + File-Organization Rules)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md (Claude's Discretion bullet 1 — README skeleton sections; bullet 3 — README hook timing for Phase 1 = empty; AUDIT-07 fills the hook in Plan 01-02)
  </read_first>
  <action>
    Create the directory tree and two markdown files.

    **(a) Directory skeleton (BOOT-01)** — Create empty `.gitkeep` files in each directory so the empty directories survive git. Per ARCHITECTURE.md lines 102-153 and PATTERNS.md "Repo skeleton directories":
      - `data/raw/.gitkeep` (data/raw/ is gitignored, but the .gitkeep itself is NOT gitignored — `.gitkeep` files do not match `data/raw/` because gitignore-listed dirs only ignore their CONTENTS unless the dir itself is excluded; in this case `data/raw/` IS the directory listing in .gitignore so the .gitkeep WILL be ignored — alternative: skip `data/raw/.gitkeep` and let Phase 2 ETL create the directory at runtime via `Path("data/raw").mkdir(parents=True, exist_ok=True)`. **Decision: skip the .gitkeep for `data/raw/`** since the directory is gitignored anyway; instead, document in `data/README.md` that ETL creates the directory.)
      - `schema/.gitkeep`, `etl/.gitkeep`, `queries/.gitkeep`, `analysis/.gitkeep`, `findings/.gitkeep`, `findings/images/.gitkeep`, `audit/.gitkeep`, `docs/.gitkeep` — these are empty .gitkeep files (zero bytes) so empty directories are tracked by git for downstream phases.

    Do NOT create a `tests/` directory — CLAUDE.md File-Organization Rules explicitly forbids it (assertions live in ETL).

    **(b) `data/README.md` stub** — 5–8 lines, memo-style (no exclamation-point punctuation, no emoji headers). Plain markdown. Content:
      ```markdown
      # Data

      The SQLite database `data/nfl_*.db` is gitignored (200–400 MB exceeds GitHub's 100 MiB hard limit).

      To regenerate from a fresh clone: `python -m etl.run` (Phase 2 deliverable; ~2–5 min on first run).

      Raw `nfl_data_py` parquet caches land in `data/raw/` (also gitignored). The ETL creates this directory at runtime.
      ```
      This is a stub — the full DOC-08 version lands in Phase 4. Do NOT add attribution / glossary / setup commands here (those belong in the root README.md per file-organization rules).

    **(c) `README.md` skeleton (BOOT-06)** — Per PATTERNS.md "README.md (skeleton, BOOT-06)" pattern + CONTEXT.md §Claude's Discretion bullet 1 (defaulted to standard portfolio README structure). Empty section bodies with HTML comments naming which phase/requirement fills them. Content (verbatim, except adjust phase/req references if they conflict with REQUIREMENTS.md):

      ```markdown
      # NFL Defensive Tendencies

      ## Hook
      <!-- 2-3 sentences, plain English, names the public-FTN pivot. Filled in Phase 1 / AUDIT-07. -->

      ## Findings preview
      <!-- 3-4 stat-first bullets with N inline. Placeholders here; numbers fill in Phase 4 / DOC-03. -->

      ## Architecture
      <!-- Mermaid diagram (data flow, not file tree). Built in Phase 4 / DOC-04. -->

      ## Setup
      <!-- 5-command block: clone, venv, pip install, python -m etl.run, jupyter. Built in Phase 4 / DOC-05. -->

      ## Glossary
      <!-- 6 football terms (down, distance, EPA, blitz, RPO, predictability index). Built in Phase 4 / DOC-06. -->

      ## Methodology
      <!-- Brief; link to findings/FINDINGS.md for the full version. Built in Phase 4. -->

      ## Limitations
      <!-- What public FTN can't tell us (no Cover 0-6 labels, charter subjectivity, etc.). Built in Phase 4. -->

      ## Attribution
      <!-- FTN Data via nflverse, CC-BY-SA 4.0; nflfastR. Per Phase 4 / DOC-07. -->

      ## Known Issues
      <!-- nfl_data_py archived 2025-09-25; v2 migration path. Per Phase 4 / DOC-07. -->
      ```

    **CRITICAL voice rules** (CLAUDE.md lines 99-107 + PATTERNS.md Shared Pattern 1):
      - NO exclamation-point punctuation anywhere in the rendered prose. (HTML comment delimiters `<!-- ... -->` are allowed — they contain `!` as part of comment syntax, not English punctuation.)
      - NO emoji section headers (no ✨, no 🚀, no anything).
      - NO "Welcome to my project!" greeting tone.
      - NO shields.io badges (PITFALLS.md / SUMMARY.md anti-features list).
      - NO claims yet — only empty headers + HTML comments. AUDIT-07 in Plan 01-02 fills the Hook with 2-3 sentences (placeholder slots for Phase 4 numbers).
      - NO "Future Work" section / laundry list.
      - NO self-deprecating tone ("This is just a small project…").
      - NO academic structure (Abstract / Methods / Results / Discussion).
  </action>
  <verify>
    <automated>test -f data/README.md && test -f README.md && test -f schema/.gitkeep && test -f etl/.gitkeep && test -f queries/.gitkeep && test -f analysis/.gitkeep && test -f findings/.gitkeep && test -f findings/images/.gitkeep && test -f audit/.gitkeep && test -f docs/.gitkeep && grep -q "^# NFL Defensive Tendencies$" README.md && grep -q "^## Hook$" README.md && grep -q "^## Findings preview$" README.md && grep -q "^## Architecture$" README.md && grep -q "^## Setup$" README.md && grep -q "^## Glossary$" README.md && grep -q "^## Methodology$" README.md && grep -q "^## Limitations$" README.md && grep -q "^## Attribution$" README.md && grep -q "^## Known Issues$" README.md && [ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ] && ! grep -q "Welcome to" README.md && grep -q "gitignored" data/README.md && grep -q "etl\.run" data/README.md && [ ! -d tests ]</automated>
  </verify>
  <acceptance_criteria>
    - All 8 `.gitkeep` files exist (schema/, etl/, queries/, analysis/, findings/, findings/images/, audit/, docs/) and are zero-byte (or near-zero — empty file)
    - `data/README.md` exists, contains the word `gitignored`, contains the string `etl.run` (referencing Phase 2 deliverable), and is at least 5 lines
    - `README.md` exists at repo root and contains the H1 `# NFL Defensive Tendencies`
    - `README.md` contains all 9 H2 section headers verbatim: `## Hook`, `## Findings preview`, `## Architecture`, `## Setup`, `## Glossary`, `## Methodology`, `## Limitations`, `## Attribution`, `## Known Issues`
    - `README.md` contains NO exclamation-point punctuation in rendered prose — verified by stripping HTML comments first and then grepping for `!`. HTML comment delimiters (`<!-- ... -->`) are explicitly allowed because their `!` is comment syntax, not English punctuation. Concretely: `[ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ]` (zero `!` occurrences in non-comment content)
    - `README.md` does NOT contain the string `Welcome to` (case-sensitive)
    - `README.md` does NOT contain emoji section headers (no characters like ✨ 🚀 📊 📈 🏈)
    - `README.md` does NOT contain shields.io badge URLs (`img.shields.io`)
    - `tests/` directory does NOT exist (CLAUDE.md File-Organization Rules forbids cargo-cult tests/)
    - No source code (`.py`, `.sql`, `.ipynb`) lives at repo root — only config + markdown
  </acceptance_criteria>
  <done>
    Directory tree is in place with .gitkeep markers; `data/raw/` is intentionally not .gitkept (gitignored, ETL creates at runtime per data/README.md). README.md skeleton has all 9 voice-compliant section headers with HTML-comment placeholders; no claims, no jargon, no emoji, no exclamation-point punctuation in rendered prose (HTML comment delimiters allowed). `data/README.md` is a 5+ line stub naming the regeneration path. No tests/ directory was created.
  </done>
</task>

<task type="auto">
  <name>Task 3: BOOT-07 — fresh-venv install verification (single serial gate for Wave 1)</name>
  <files>(no files modified — verification only; produces console output captured by orchestrator)</files>
  <read_first>
    - requirements.txt (created in Task 1 — must exist before this task runs)
    - .python-version (must contain 3.11)
    - .planning/REQUIREMENTS.md (BOOT-07 acceptance criteria)
    - .planning/phases/01-foundation-ftn-pivot-calibration/01-CONTEXT.md (§code_context — venv/ exists at repo root from /gsd-init; clarify reuse vs fresh)
    - .planning/research/PITFALLS.md (Pitfall #1 — Python 3.13 install fails; Pitfall #8 — pin tightly; Pitfall #16 — verify install on fresh venv before ship)
    - CLAUDE.md (Locked Stack — Python 3.11 only)
  </read_first>
  <action>
    Verify the locked stack actually installs on a clean Python 3.11 venv. This is BOOT-07 — the single serial gate for Wave 1. Plan 01-02 cannot start until this passes (the audit notebook calls `import nfl_data_py`).

    **Strategy:** Create a FRESH `.venv-bootcheck/` (NOT reuse the existing `venv/` from /gsd-init — that may have stale packages or be on a different Python version). Use this fresh venv only for verification, then leave it in place (gitignored via `venv/` and `.venv/` rules — note `.venv-bootcheck` matches neither glob; explicitly ignore via temporary rule OR clean it up after verification — choose: clean it up at end of task to avoid polluting working tree).

    **Steps (sequential):**

    1. **Confirm Python 3.11 is available.** Run:
       ```bash
       python --version 2>&1 | grep -E "Python 3\.11\."
       ```
       If exit nonzero, fail with a checkpoint asking the user to install Python 3.11 (per CLAUDE.md Locked Stack — 3.13 install fails on `nfl_data_py==0.3.3` per upstream issue #122).

    2. **Create the fresh venv.** Use Python 3.11 explicitly:
       ```bash
       python -m venv .venv-bootcheck
       ```
       On Windows (per env: shell is bash but project is on Win32), the activation script lives at `.venv-bootcheck/Scripts/activate` (not `bin/activate`). Use the Python interpreter directly to avoid activation cross-platform headaches:
       ```bash
       .venv-bootcheck/Scripts/python.exe -m pip install --upgrade pip
       ```

    3. **Install the pinned stack.**
       ```bash
       .venv-bootcheck/Scripts/python.exe -m pip install -r requirements.txt
       ```
       Capture both stdout and stderr. The install MUST exit 0 with no `ERROR` lines (warnings about deprecation are acceptable — `nfl_data_py` archive notice is expected).

    4. **Verify `import nfl_data_py` works.**
       ```bash
       .venv-bootcheck/Scripts/python.exe -c "import nfl_data_py; print('nfl_data_py version:', nfl_data_py.__version__ if hasattr(nfl_data_py, '__version__') else 'imported ok')"
       ```
       Must exit 0.

    5. **Verify the `np.float_` constraint holds** (per PITFALLS.md Pitfall #16 — the specific landmine):
       ```bash
       .venv-bootcheck/Scripts/python.exe -c "import numpy as np; assert np.__version__.split('.')[0] == '1', f'numpy {np.__version__} is not <2 — pin failed'; print('numpy', np.__version__, 'is <2 ✓')"
       ```
       Must exit 0. (NOTE: per voice rules, do NOT include the ✓ in any committed file — this is console output only, ephemeral.)

    6. **Verify `ruff` runs clean against the empty/skeleton repo.**
       ```bash
       .venv-bootcheck/Scripts/python.exe -m ruff check .
       ```
       Must exit 0 (or 0 errors, given the repo has no `.py` files yet — this just confirms ruff is installed and config is valid). If ruff exits with config errors, the `pyproject.toml` from Task 1 has a problem.

    7. **Cleanup.** Remove `.venv-bootcheck/` after verification:
       ```bash
       rm -rf .venv-bootcheck
       ```
       Do NOT commit `.venv-bootcheck` — neither `venv/` nor `.venv/` gitignore entries match it. The cleanup is required to avoid polluting the working tree.

    **Failure handling:**
      - If pip install fails: capture the full error output. Common failures (per PITFALLS.md #1, #8, #16): wrong Python version (3.12/3.13 instead of 3.11), pyarrow wheel unavailable for the Python version, numpy resolution conflict. Surface the error to the orchestrator with a clear remediation step. Do NOT modify `requirements.txt` to "fix" the install — the pin block is locked per SUMMARY.md.
      - If `import nfl_data_py` fails after a successful install: this is the `np.float_` Path A landmine. Verify `numpy<2` is actually installed (`pip show numpy`); if numpy 2.x got resolved despite the pin, the pin is wrong — re-check Task 1 acceptance criteria.
  </action>
  <verify>
    <automated>python --version 2>&1 | grep -qE "Python 3\.11\." && python -m venv .venv-bootcheck && .venv-bootcheck/Scripts/python.exe -m pip install --upgrade pip --quiet && .venv-bootcheck/Scripts/python.exe -m pip install -r requirements.txt --quiet && .venv-bootcheck/Scripts/python.exe -c "import nfl_data_py" && .venv-bootcheck/Scripts/python.exe -c "import numpy as np; assert np.__version__.split('.')[0] == '1'" && .venv-bootcheck/Scripts/python.exe -c "import pandas; assert pandas.__version__.startswith('2.1') or pandas.__version__.startswith('2.2')" && .venv-bootcheck/Scripts/python.exe -m ruff check . && rm -rf .venv-bootcheck</automated>
  </verify>
  <acceptance_criteria>
    - `python --version` reports `Python 3.11.x` (any patch version on the 3.11 line)
    - `pip install -r requirements.txt` exits 0 in the fresh `.venv-bootcheck/` (warnings allowed, errors not)
    - `python -c 'import nfl_data_py'` exits 0
    - `python -c 'import numpy as np; assert np.__version__.split('.')[0] == "1"'` exits 0 (numpy major version is 1, not 2)
    - `python -c "import pandas; assert pandas.__version__.startswith('2.1') or pandas.__version__.startswith('2.2')"` exits 0 (pandas is in the 2.1.x or 2.2.x range, NOT 2.3+)
    - `python -m ruff check .` exits 0 (config is valid; no ruff config errors against the skeleton repo)
    - `.venv-bootcheck/` directory is removed at end of task (not left polluting the working tree)
    - Task is the LAST task in Plan 01-01 (single serial gate at end of Wave 1) — no other Plan 01-01 task runs after this
  </acceptance_criteria>
  <done>
    A fresh Python 3.11 venv successfully installed `requirements.txt`, `import nfl_data_py` succeeded, `numpy<2` constraint held, `pandas` resolved to the 2.1.x or 2.2.x band, `ruff check .` exited 0. The verification venv was removed. Plan 01-02 (FTN Audit) can now safely call `nfl.import_ftn_data([2022, 2023, 2024])` against the locked stack.
  </done>
</task>

</tasks>

<verification>
Phase-level checks across all three tasks:

```bash
# Frontmatter / structural
test -f requirements.txt && test -f pyproject.toml && test -f .python-version && test -f .gitignore
test -f README.md && test -f data/README.md
test -d schema -a -d etl -a -d queries -a -d analysis -a -d findings -a -d findings/images -a -d audit -a -d docs
[ ! -d tests ]  # CLAUDE.md forbids cargo-cult tests/

# Stack integrity
grep -q "^nfl_data_py==0.3.3$" requirements.txt
grep -q "^numpy>=1.26,<2.0" requirements.txt
grep -q "^pandas>=2.1,<2.3$" requirements.txt
grep -q "^seaborn==0.13.2" requirements.txt
grep -q '\[tool\.ruff\]' pyproject.toml
grep -q 'target-version = "py311"' pyproject.toml
[ "$(cat .python-version | tr -d '[:space:]')" = "3.11" ]

# Public-repo discipline
grep -q "^\*\.db$" .gitignore
grep -q "^\.env\*$" .gitignore
grep -q "^scratch/$" .gitignore
! grep -q "^\.env$" .gitignore  # bare .env replaced by .env* glob

# README voice rules
grep -q "^# NFL Defensive Tendencies$" README.md
grep -q "^## Hook$" README.md
grep -q "^## Known Issues$" README.md
# No exclamation-point punctuation in rendered prose; HTML comment delimiters allowed
[ "$(sed -E 's/<!--[^>]*-->//g' README.md | grep -c '!')" -eq 0 ]
! grep -q "Welcome to" README.md

# Install verification (Task 3 — must have run successfully and cleaned up)
[ ! -d .venv-bootcheck ]
```
</verification>

<success_criteria>
1. **BOOT-01:** Directory tree from `.planning/research/ARCHITECTURE.md` exists (data/, data/raw/, schema/, etl/, queries/, analysis/, findings/, findings/images/, audit/, docs/), with `.gitkeep` markers on the empty directories. NO `tests/` directory.
2. **BOOT-02:** `requirements.txt` pins the 14 packages from SUMMARY.md Consolidated Stack verbatim. `pandas>=2.1,<2.3` (not STACK.md's drift).
3. **BOOT-03:** `.gitignore` hard-excludes `*.db`, uses `.env*` glob, includes `scratch/`, retains all existing entries.
4. **BOOT-04:** `pyproject.toml` contains `[tool.ruff]` config only; no `[project]` or `[build-system]`.
5. **BOOT-05:** `.python-version` contains exactly `3.11`.
6. **BOOT-06:** `README.md` skeleton has 9 voice-compliant section headers; no claims, no exclamation-point punctuation in rendered prose, no emoji headers, no "Welcome to my project!". (HTML comment delimiters `<!-- ... -->` are explicitly allowed because their `!` is comment syntax, not English punctuation.)
7. **BOOT-07:** Fresh Python 3.11 venv installs `requirements.txt` cleanly; `import nfl_data_py` exits 0; `numpy<2` constraint holds; `ruff check .` exits 0; verification venv cleaned up.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → public GitHub repo | All files staged for ship phase cross this boundary; secrets / large data must be filtered before push |
| recruiter laptop → PyPI | `pip install` resolves package versions transitively; supply-chain compromise of any pinned package would execute on recruiter's machine |
| nflverse CDN → recruiter laptop (Phase 2 ETL, deferred) | Out of scope for Plan 01-01; flagged for Phase 2 threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `.env*` files | mitigate | `.gitignore` uses `.env*` glob (Task 1) — catches `.env`, `.env.local`, `.env.production`, etc. Verified by acceptance criteria. |
| T-01-01-02 | Information Disclosure | Committed credentials in scratch files | mitigate | `.gitignore` excludes `scratch/`, `scratch*`, `tmp_*` (Task 1). Plus CLAUDE.md File-Organization Rules forbid scratch in repo root. |
| T-01-01-03 | Information Disclosure | Committed `.db` (could contain PII or proprietary derived data) | mitigate | `.gitignore` hard-excludes `*.db` (Task 1, replaces deferral comment per Pitfall #14). |
| T-01-01-04 | Tampering | Unpinned dependency resolves to malicious version | mitigate | `requirements.txt` uses tight version ranges or exact pins for all 14 packages (BOOT-02). `numpy<2.0` is the most-protective lower bound. |
| T-01-01-05 | Denial of Service | `nfl_data_py` archived; future PyPI removal makes install fail | accept | User locked Path A in PROJECT.md Key Decisions; documented v2 migration path to `nflreadpy` (deferred to V2-LIB-01). README "Known Issues" section (Phase 4) discloses. |
| T-01-01-06 | Tampering | Recruiter's `pip install` resolves NumPy 2.0, breaks `np.float_` reference in `nfl_data_py` 0.3.x | mitigate | Task 1 pins `numpy>=1.26,<2.0`. Task 3 verifies install on fresh venv before phase completes. |
| T-01-01-07 | Spoofing | Bootstrap phase commits hooks or PyPI URLs without provenance | accept | Project has no pre-commit framework (per STACK.md research recommendation); ruff runs locally; no third-party hook installation in this phase. |
| T-01-01-08 | Information Disclosure | `data/raw/` parquet caches commit accidentally | mitigate | `.gitignore` includes `data/raw/` (already present from existing `.gitignore`, retained in Task 1). Also `data/*.parquet` retained. |
</threat_model>

<output>
After completion, create `.planning/phases/01-foundation-ftn-pivot-calibration/01-01-SUMMARY.md` documenting:
- Files created (15 — 4 config + 8 .gitkeep + 2 markdown + 1 README)
- BOOT-01..07 acceptance criteria evidence
- Whether existing `venv/` was reused or a fresh `.venv-bootcheck/` was created (per acceptance: fresh, then cleaned up)
- Pip install timing (≤ 60s expected on warm pip cache; 2-3 min cold)
- Any deviation from the locked pin block in `.planning/research/SUMMARY.md` (should be NONE — flag for orchestrator review if any drift)
- Confirmation that `tests/` directory was NOT created
</output>
