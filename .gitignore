# Python
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Jupyter
*.ipynb_checkpoints

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Data — large raw pulls + SQLite DB are gitignored
# (.db locked gitignored per Pitfall #14: 200–400 MB exceeds GitHub's 100 MiB hard limit;
# recruiter regenerates via `python -m etl.run`)
data/raw/
data/*.db-journal
data/*.parquet
*.db

# Secrets (defensive, even though this project shouldn't have any)
.env*
*.key
*.pem

# Scratch / temp
scratch/
scratch*
tmp_*
*.log
